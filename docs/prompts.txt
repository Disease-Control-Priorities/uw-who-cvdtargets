write a promt to ask claude to conduct this task You are going to modify the aim1_report.RMD file to add burden of disease results and a break down by who region and country income group according to World bank classification
The file 07_output_dalys include the production of yll, yld and dalys by intervention, that is the file to use to report burden of disease
# save RDS to compute deaths delayed and burden of disease averted in report
saveRDS(dt, paste0(wd_outp, "dt_output_dalys.rds"))
 you will only modify the aim1_report.RMD to
Add: table of period 2026-2050 cumulative deaths, ylls, ylds and dalys averted. By intervention scenario
table of period 2026-2050 cumulative deaths, ylls, ylds and dalys averted. By intervention scenario and cause. Add % contribution To reduction in deaths and dalys
Table by who region 2026-2050 cumulative deaths, ylls, ylds and dalys averted. By intervention scenario
table by income group and intervention scenario 2026-2050 cumulative deaths, ylls, ylds and dalys averted.  
Figure 2026-2050 cumulative dalys averted time series. By intervention scenario
Figure 2026-2050 cumulative dalys averted. Who region all interventions combined
Only modify aim1_report.RMD


##########################################################################333
##################333

Here's the prompt:

---

You are working on an epidemiological modeling manuscript for publication. Your task is to modify two files: **`who_cvd_targets_paper1.RMD`** (primary) and **`aim1_report.RMD`** (secondary, only if additional calculations are needed). Do not modify any other file.

## Manuscript Target

- **Journal style:** The Lancet
- **References:** APA Style (American Psychological Association)
- **Bibliography file:** `C:\Users\wrgar\OneDrive - UW\02Work\WHO-CVD\github\uw-who-cvdtargets\docs\references.bib`

---

## Context Files — Read These First Before Writing Anything

Before modifying any file, read and understand the following:

| File | Purpose |
|---|---|
| `aim1_report.RMD` | Source of all computed outputs — tables, figures, and inline values. All R objects live here. |
| `aim1_executive_slides.RMD` | Narrative guide — use this to understand the storyline, framing, and key messages for results and discussion |
| `06_run_scenarios_multiple.R` | The executed model — use this for methodological context and to understand scenario definitions, parameters, and model structure |
| `references.bib` | Bibliography — draw from this for all in-text citations and the reference list |

---

## Your Task

Fill the following sections of `who_cvd_targets_paper1.RMD` in Lancet style. Each section is described below.

---

### Results

Write a structured results narrative that:

- Leads with the overall scale of impact across all scenarios and the full 2026–2050 horizon
- Progresses logically: overall burden averted → by intervention scenario → by cause → by geography (WHO region) → by World Bank income group
- Cross-references every table and figure by number inline (e.g., "Table 1", "Figure 2") — do not embed raw numbers without a table/figure reference
- Uses precise epidemiological language consistent with Lancet standards (absolute counts with uncertainty ranges where available, relative reductions, cumulative metrics)
- Does not contain any R code chunks or object execution — all values must be pulled as inline references from objects already computed and named in `aim1_report.RMD`
- If a needed summary statistic or inline value does not yet exist as a named object in `aim1_report.RMD`, add the calculation there and reference it here — do not compute inside `who_cvd_targets_paper1.RMD`

---

### Discussion

Write a discussion section structured as follows, consistent with Lancet conventions:

1. **Summary of principal findings** — one concise paragraph restating the headline results without raw numbers
2. **Interpretation in context** — compare findings to existing literature; cite relevant references from `references.bib`; situate results within the WHO CVD targets policy landscape
3. **Mechanisms and plausibility** — explain why certain interventions or regions show larger gains; draw on model structure from `06_run_scenarios_multiple.R` for mechanistic reasoning
4. **Equity implications** — discuss findings by WHO region and World Bank income group; highlight disparities and their policy relevance
5. **Strengths and limitations** — address model assumptions, data sources, generalizability, and uncertainty
6. **Conclusion** — end with a clear, policy-relevant closing statement appropriate for a Lancet audience

All claims must be supported by citations from `references.bib` in APA in-text format (Author, Year).

---

### Tables

Insert the following tables in the Tables section of `who_cvd_targets_paper1.RMD`. Each table must:

- Be sourced by referencing the corresponding named object already rendered in `aim1_report.RMD` — use `\@ref()` cross-references, not re-executed code
- Have a **bold title** and a **standalone descriptive caption** beneath it that allows the table to be understood without reading the main text (include intervention definitions, period, population, and metric definitions in the note)
- Follow Lancet table style: clean borders, no vertical lines, grouped rows where applicable

| Table # | Content |
|---|---|
| Table 1 | Cumulative 2026–2050 deaths, YLLs, YLDs, and DALYs averted by intervention scenario |
| Table 2 | Cumulative 2026–2050 deaths, YLLs, YLDs, and DALYs averted by intervention scenario and cause, with % contribution to total reduction |
| Table 3 | Cumulative 2026–2050 deaths, YLLs, YLDs, and DALYs averted by WHO region and intervention scenario |
| Table 4 | Cumulative 2026–2050 deaths, YLLs, YLDs, and DALYs averted by World Bank income group and intervention scenario |

---

### Figures

Insert the following figures in the Figures section. Each figure must:

- Reference the object rendered in `aim1_report.RMD` using `\@ref()` — do not re-execute ggplot code inside `who_cvd_targets_paper1.RMD`
- Have a **figure title** and a **standalone descriptive legend** below it explaining what is shown, how to read it, what the colors/lines represent, and what the time horizon and population are
- Follow Lancet figure style conventions (clean background, accessible color palette, labeled axes)

| Figure # | Content |
|---|---|
| Figure 1 | Time series of cumulative DALYs averted 2026–2050 by intervention scenario |
| Figure 2 | Cumulative DALYs averted by WHO region, all interventions combined |

---

## Rules

- **Only modify `who_cvd_targets_paper1.RMD` and `aim1_report.RMD`**
- Never add R chunk execution to `who_cvd_targets_paper1.RMD` — it is a manuscript file only
- If a calculation is missing from `aim1_report.RMD`, add it there with a clearly named object, then reference it in the manuscript
- All citations must use keys from `references.bib` and render in APA format
- Do not alter the Methods section or any front matter (title, authors, abstract) unless explicitly instructed
- Preserve all existing chunk labels, YAML headers, and cross-reference infrastructure in both files

##########################################################################################################
########################################################################################################33

You are making targeted structural and content edits to the manuscript file 
`who_cvd_targets_paper1.RMD`. Modify ONLY this file. Do not touch any other file.

---

## FILE TO MODIFY
- `who_cvd_targets_paper1.RMD` only

## FILES TO READ BUT NOT MODIFY (context only)
- `aim1_report.RMD` — to verify figure chunk names for correct cross-referencing

---

## CHANGE 1 — DELETE ONE FIGURE ENTIRELY

Remove the following figure completely from the manuscript:
- "Top 20 countries by projected CVD deaths averted under the combined 
  intervention scenario, 2025–2050, coloured by WHO region"

This means:
- Delete its R chunk (the chunk that calls or displays this figure)
- Delete its figure title and subcaption
- Delete any cross-reference to it in the main text (e.g., any 
  `\@ref(fig:...)` pointing to this figure's chunk label)
- Delete any prose in Results or Discussion that describes country-level 
  findings derived from this figure
- Do not leave a blank line or comment where the chunk was — close the gap cleanly

---

## CHANGE 2 — REMOVE ALL COUNTRY-LEVEL DESCRIPTION FROM MAIN TEXT

Audit the entire Results and Discussion sections for any sentences or 
paragraphs that describe findings by individual country (e.g., naming 
specific countries, ranking countries, comparing country-level estimates).

- Remove all such country-level descriptions from Results and Discussion
- If a sentence is partially country-level (e.g., "Globally, deaths were 
  reduced, with India and China accounting for..."), trim only the 
  country-specific portion and preserve the global/regional claim
- Do not remove regional-level (WHO region) descriptions — only country-named content

---

## CHANGE 3 — RESTRUCTURE THE FIGURES SECTION

### Main Figures section must contain ONLY these two figures:
  1. Cumulative CVD deaths averted over time by intervention scenario
  2. Distribution of projected CVD deaths averted by age group, sex, 
     and intervention scenario

All other figures currently in the Main Figures section must be moved 
to the Supplemental Material section (see Change 4).

### Steps:
- Retain the two figures above in the Main Figures section, in this order
- Number them Figure 1 and Figure 2 respectively in their titles
- Preserve their existing R chunk labels exactly — do not rename chunks, 
  as chunk labels are used for cross-referencing
- Preserve their subcaptions exactly as written

---

## CHANGE 4 — MOVE REMAINING FIGURES TO SUPPLEMENTAL MATERIAL

All figures that were in the Main Figures section but are NOT the two 
figures listed in Change 3 must be relocated to the Supplemental Material 
section.

### Steps:
- Move each figure chunk and its title/subcaption block to the 
  Supplemental Material section, preserving chunk labels exactly
- Renumber these figures sequentially as Supplemental Figure S1, S2, S3... 
  in their title text (e.g., "Supplemental Figure S1. ...")
- Do not rename chunk labels — only update the display title text
- If a Supplemental Material section does not already exist in the file, 
  create one at the end of the document with the heading:
  `# Supplemental Material`
  followed by the subheading:
  `## Supplemental Figures`

### Rewrite Supplemental Figure descriptions:
- For each figure moved to Supplemental Material, rewrite its subcaption 
  to be fully self-contained for a reader who reads supplemental material 
  independently — include enough context about the scenario, population, 
  time horizon, and metric so the figure is interpretable without reading 
  the main text
- Keep the scientific content accurate; only expand for standalone clarity

---

## CHANGE 5 — UPDATE ALL CROSS-REFERENCES IN MAIN TEXT

After completing Changes 1–4, audit every figure cross-reference in 
the entire manuscript (Results, Discussion, and any other section).

### Rules:
- All cross-references must use bookdown-style syntax:
  `(Figure \@ref(fig:chunk-label))`
  For example: `(Figure \@ref(fig:fig-bau-deaths))`
- Cross-references to the two main figures must read "Figure 1" and 
  "Figure 2" — confirm the chunk labels resolve to these correctly
- Cross-references to supplemental figures must read 
  "Supplemental Figure S1", "Supplemental Figure S2", etc. — update 
  all corresponding `\@ref()` calls and surrounding text accordingly
- Remove any cross-references that pointed to the deleted figure 
  (Change 1) — do not leave orphaned `\@ref()` calls
- Do not use hardcoded figure numbers anywhere in prose — all figure 
  numbers in text must flow through `\@ref()` cross-references

---

## CHANGE 6 — RENUMBER FIGURE TITLES CONSISTENTLY

After all moves and deletions are complete, verify that every figure 
title in the document uses the correct sequential number matching its 
position and section:

- Main Figures section: Figure 1, Figure 2 (in order of appearance)
- Supplemental Material section: Supplemental Figure S1, S2, S3... 
  (in order of appearance)
- No figure number should be skipped or duplicated
- The number in the title text and the number implied by the 
  cross-reference in the main text must match

---

## CONSTRAINTS
- Modify ONLY `who_cvd_targets_paper1.RMD`
- Do not rename, modify, or add any R chunk labels — chunk labels are 
  the anchor for `\@ref()` and must remain stable
- Do not alter any table chunks, table titles, or table cross-references
- Do not alter the Methods or Abstract sections
- Do not alter any prose in Results or Discussion except to:
  (a) remove country-level descriptions (Change 2)
  (b) update figure cross-references (Change 5)
- Do not add new analytical content or interpret results differently

---

## BEFORE YOU BEGIN
1. Read the entire `who_cvd_targets_paper1.RMD` file from top to bottom
2. List every figure currently in the file: its chunk label, its current 
   title/number, and its current location (Main or Supplemental)
3. Identify the chunk label of the figure to be deleted (Change 1)
4. Identify which two chunk labels correspond to the two figures 
   that stay in Main Figures (Change 3)
5. Identify all remaining figure chunk labels to be moved (Change 4)
6. Produce this inventory as an internal checklist before making any edits

Only after completing the inventory should you begin editing the file.
Produce a summary of all changes made at the end.


#####################################################################################
#####################################################################################

You are making targeted mathematical and structural edits to the manuscript file
`who_cvd_targets_paper1.RMD`. Modify ONLY this file. Do not touch any other file.

---

## FILE TO MODIFY
- `who_cvd_targets_paper1.RMD` only

## FILES TO READ BUT NOT MODIFY (context only)
- `06_run_scenarios_multiple.R` — SOURCE OF TRUTH for all mathematical logic,
  parameter names, transformations, and model structure. What is implemented
  here governs what appears in the manuscript. This overrides any verbal
  description currently in the manuscript.
- `math-doc.RMD` — related project notation reference. Use for LaTeX formatting
  conventions and symbol choices where they are consistent with
  06_run_scenarios_multiple.R. Where they conflict, defer to the R file.
- `aim1_report.RMD` — for parameter values, data sources, and uncertainty
  distributions actually used in the model runs

---

## OVERARCHING RULES

- Mathematical documentation must be internally consistent across the entire
  manuscript — every symbol introduced must be defined once, used consistently,
  and never redefined with a different meaning elsewhere in the document
- The main manuscript text (Abstract, Introduction, Methods narrative, Results,
  Discussion) must cross-reference the Mathematical Details section for formal
  derivations, e.g.:
  "(see Mathematical Details: Multi-State Transition Model)"
  or
  "(Mathematical Notation Summary, Table A)"
  Add these cross-references wherever the main text describes a process that
  is formally specified in the math section — do not duplicate the math in
  the narrative, only reference it
- All formulas must render correctly in a Word document output (word_document
  or officedown::rdocx_document). Use only pandoc-compatible LaTeX math:
  inline math with `$...$` and display math with `$$...$$`
  Do NOT use raw HTML math, MathJax-specific syntax, or LaTeX environments
  that do not compile through pandoc (e.g., \begin{align} is acceptable;
  \begin{equation*} with AMS extensions may not be — test conservatively)

---

## TASK — RESTRUCTURE AND COMPLETE THE MATHEMATICAL DETAILS SECTION

Locate the existing "Methodology and mathematical details" section in
`who_cvd_targets_paper1.RMD`. Reorganize it and fill it according to the
five subsections below. Preserve any existing correct formulas; complete
or correct any that are missing symbols, incomplete, or only verbally described.

The section heading and all five subsections must appear in this exact order:

# Methodology and Mathematical Details

## Mathematical Notation Summary

## Key Parameter Appendix

## Multi-State Transition Model

## Hypertension Control Intervention Model

## Statin Therapy Intervention Model

---

### SUBSECTION 1 — Mathematical Notation Summary

Purpose: Define every symbol used anywhere in the mathematical sections,
in one consolidated reference table.

- Create a two-column display table (Symbol | Definition) listing every
  mathematical symbol, subscript, and superscript used across all subsections
- Format as a Word-compatible table (use flextable or kable with
  format = "pandoc" — whichever is already standard in this manuscript)
- Include: state variables, transition rates, time indices, intervention
  effect parameters, country/age/sex indices, and any summary output
  metrics (e.g., deaths averted, DALYs)
- Derive symbol names directly from variable and parameter names in
  `06_run_scenarios_multiple.R` — map R object names to LaTeX symbols
  and document that mapping explicitly in a comment in the RMD chunk
- Do not introduce symbols that do not correspond to something implemented
  in the R code
- Add a sentence at the top of this subsection directing readers:
  "All symbols used in subsequent subsections are defined in Table [X] below."

---

### SUBSECTION 2 — Key Parameter Appendix

Purpose: Provide a complete reference table of all base parameter values
used in the model.

- Create a multi-column Word-compatible table with these columns:
  Parameter | Symbol | Base Value | Unit | Data Source | Transformation Applied |
  Uncertainty Distribution
- Populate by reading `06_run_scenarios_multiple.R` for parameter names
  and transformations, and `aim1_report.RMD` for base values, data sources,
  and uncertainty specifications
- "Transformation Applied" column must describe any mathematical operation
  applied to the raw data value before it enters the model (e.g.,
  "log-transformed", "converted from annual to monthly rate via 1-(1-r)^(1/12)",
  "age-standardized per 100,000")
- "Uncertainty Distribution" column must state the distributional assumption
  used in sensitivity or probabilistic analyses (e.g., "Beta(α, β)",
  "Normal(μ, σ)", "Fixed — no uncertainty applied")
- If a parameter has no uncertainty distribution in the model, state
  "Deterministic" explicitly — do not leave the cell blank
- Sort rows by subsection: first transition model parameters, then
  hypertension intervention parameters, then statin intervention parameters
- Add a footnote below the table listing all data sources in APA format,
  keyed by a superscript in the Data Source column

---

### SUBSECTION 3 — Multi-State Transition Model

Purpose: Formally specify the disease progression model structure.

Read `06_run_scenarios_multiple.R` carefully and extract:
- The health states modeled (e.g., Healthy, Hypertensive, CVD event,
  CVD death, Non-CVD death — use whatever states are actually coded)
- All transition probabilities or rates between states
- The time step used (annual, monthly, etc.)
- Any age-, sex-, or country-specific stratification applied to transitions
- Any baseline mortality or background hazard applied

Write this subsection to include:

1. A state diagram described in text (e.g., "The model comprises N health
   states: [list]. Individuals transition between states according to..."
   — do not attempt to render a graphical diagram in Word)
2. The core transition equation(s) in display math `$$...$$`, using symbols
   defined in the Notation Summary
3. Explicit notation for time indexing, cohort stratification (age, sex,
   country), and cycle length
4. The formula for computing the primary outcome (CVD deaths averted)
   expressed as the difference between scenario and counterfactual state
   occupancy
5. A cross-reference at the end: "(Parameter values for all transition
   rates are listed in the Key Parameter Appendix)"

If any formula currently in the manuscript is only verbally described,
replace the verbal description with the proper LaTeX display equation.
If any formula is missing a symbol (renders as a blank or placeholder),
identify the correct symbol from the R code and insert it.

---

### SUBSECTION 4 — Hypertension Control Intervention Model

Purpose: Formally specify how hypertension treatment affects model transitions,
including the diabetes component if it is modeled jointly.

Read `06_run_scenarios_multiple.R` and extract:
- How hypertension control coverage is parameterized (target coverage,
  scale-up function, time trajectory)
- How achieving hypertension control modifies transition probabilities
  (e.g., relative risk reduction applied to which transitions)
- If diabetes is modeled as a joint risk factor or separate state,
  specify its interaction with hypertension control explicitly

Write this subsection to include:

1. The coverage scale-up equation in display math, showing how coverage
   C(t) changes over time from baseline to target
2. The relative risk or rate ratio applied to transition probabilities
   under treatment, in display math
3. The net effect equation showing how modified transition probabilities
   are computed from baseline rates and the intervention effect
4. If diabetes is included: a clearly labeled sub-subsection
   "Diabetes Component" that specifies whether diabetes acts as a
   modifier of hypertension transitions, a parallel state, or a separate
   risk multiplier — derive this entirely from what is coded in the R file
5. A cross-reference back to the Multi-State Transition Model subsection
   for the baseline transition structure
6. A cross-reference forward to the Key Parameter Appendix for all
   intervention effect sizes and their sources

---

### SUBSECTION 5 — Statin Therapy Intervention Model

Purpose: Formally specify how statin therapy affects model transitions.

Read `06_run_scenarios_multiple.R` and extract:
- How statin eligibility is defined (age thresholds, risk thresholds,
  or other criteria actually coded)
- How statin coverage is parameterized (target coverage, scale-up)
- How statin therapy modifies transition probabilities (relative risk
  reduction, which transitions are affected)
- Whether statin effects interact with hypertension control effects
  (additive, multiplicative, independent — use what is coded)

Write this subsection to include:

1. The eligibility definition in formal notation
2. The statin coverage scale-up equation in display math
3. The relative risk or hazard ratio applied under statin therapy,
   in display math
4. The combined intervention equation if statin and hypertension
   effects are combined (showing whether effects are multiplicative
   or additive as implemented in the R code)
5. A cross-reference back to the Hypertension subsection for the
   coverage scale-up structure if the same functional form is used
6. A cross-reference forward to the Key Parameter Appendix for all
   statin effect sizes and their sources

---

## CHANGE — ADD CROSS-REFERENCES IN MAIN TEXT

After completing the math section, audit the main manuscript text
(Methods narrative, Results, Discussion) and insert cross-references
to the math subsections wherever the text describes:

- The disease model structure or health states
  → add: "(see Multi-State Transition Model)"
- Hypertension treatment coverage or effect
  → add: "(see Hypertension Control Intervention Model)"
- Statin eligibility, coverage, or effect
  → add: "(see Statin Therapy Intervention Model)"
- Any specific parameter value, data source, or uncertainty range
  → add: "(see Key Parameter Appendix)"
- Any symbol defined in the notation table
  → add: "(see Mathematical Notation Summary)"

Use bookdown-style section cross-references where the document structure
supports it (e.g., `\@ref(sec:label)`), otherwise use plain parenthetical
text references. Do not duplicate mathematical content in the narrative —
only cross-reference it.

---

## CONSTRAINTS
- Modify ONLY `who_cvd_targets_paper1.RMD`
- All math must be derived from what is actually implemented in
  `06_run_scenarios_multiple.R` — do not invent parameters, states,
  or equations not present in the code
- Do not alter figure chunks, table chunks, Results prose (except to
  add cross-references), Discussion prose (except to add cross-references),
  Abstract, or Introduction
- Do not alter the YAML header
- All new tables in this section must use the same Word-compatible
  table package already established in the manuscript
- Symbol definitions in the Notation Summary must be exactly consistent
  with symbol usage in all three model subsections — audit for consistency
  after drafting all subsections

---

## BEFORE YOU BEGIN
1. Read `06_run_scenarios_multiple.R` completely and extract:
   (a) all health states
   (b) all transition parameters and their R names
   (c) intervention effect parameters for hypertension and statins
   (d) coverage scale-up functions
   (e) how combined scenarios are constructed
   (f) the primary outcome computation
2. Read `math-doc.RMD` and note which notation conventions are
   consistent with the R code and which conflict
3. Read the current Methodology and Mathematical Details section in
   `who_cvd_targets_paper1.RMD` and catalog:
   (a) formulas already present and correct
   (b) formulas present but incomplete or missing symbols
   (c) content described only verbally that needs a formal equation
4. Read `aim1_report.RMD` for base parameter values, data sources,
   and uncertainty specifications
5. Produce an internal inventory of all of the above before writing
   any LaTeX or editing any prose

Only after completing this inventory should you begin editing the file.
Produce a summary of all changes made at the end, organized by subsection.

############################################################################
############################################################################

You are making targeted additions to two manuscript files to incorporate 
burden of disease metrics (YLL, YLD, DALY). You will modify exactly two files.
Do not touch any other file.

---

## FILES YOU MAY MODIFY
- `who_cvd_targets_paper1.RMD` — main manuscript
- `aim1_executive_slides.RMD` — executive summary slide deck

## FILES TO READ BUT NOT MODIFY (context only)
- `07_output_dalys.R` — SOURCE OF TRUTH for how YLL, YLD, and DALY are
  computed. All mathematical descriptions must reflect what is actually
  implemented here. This overrides any verbal description or assumption.
- `aim1_report.RMD` — SOURCE OF TRUTH for all computed DALY/YLL/YLD output
  objects and numeric results. Numbers consumed by both manuscript and slides
  must come from objects already defined here. Do not compute anything new
  in the manuscript or slides.
- `math-doc.RMD` — notation reference (use for LaTeX conventions where
  consistent with 07_output_dalys.R; defer to R file where they conflict)
- `06_run_scenarios_multiple.R` — model context for intervention scenarios
  under which DALYs are computed

---

## OVERARCHING RULES

- No calculations of any kind may be added to `who_cvd_targets_paper1.RMD`
  or `aim1_executive_slides.RMD`
- All numeric results displayed in either file must be consumed from objects
  defined and computed in `aim1_report.RMD`
- Before modifying either file, read `07_output_dalys.R` completely and
  extract the exact computational logic — do not paraphrase or reconstruct
  from general epidemiological knowledge
- Mathematical content must render correctly in Word (pandoc LaTeX:
  `$...$` inline, `$$...$$` display — no HTML math, no MathJax extensions)
- Slide content must render correctly in the existing slide format of
  `aim1_executive_slides.RMD` (preserve whatever output format, theme,
  and slide separator syntax is already in use)
- All symbols introduced must be consistent with notation already
  established in the Mathematical Details section of
  `who_cvd_targets_paper1.RMD`

---

## TASK 1 — MODIFICATIONS TO who_cvd_targets_paper1.RMD

### 1A — Methods Section: Outcomes Paragraph

Locate the outcomes or statistical analysis paragraph in the Methods section.

Add a paragraph stating that burden of disease was quantified using three
metrics: Years of Life Lost (YLL), Years Lived with Disability (YLD), and
Disability-Adjusted Life Years (DALY). The paragraph must:

- State that DALYs = YLL + YLD
- Name the specific life table standard used (extract from 07_output_dalys.R —
  e.g., Global Burden of Disease standard life table, WHO life table, or
  other — use only what is coded, do not assume)
- Name the specific disability weight source used (extract from
  07_output_dalys.R — e.g., GBD 2019 disability weights, WHO weights —
  use only what is coded)
- State the age weighting and time discounting assumptions applied,
  if any (extract from 07_output_dalys.R — if no discounting is applied,
  state that explicitly)
- Add a cross-reference directing readers to the Supplemental Mathematical
  Details for full derivation:
  "(see Supplemental Material: DALY Calculation Methods)"
- Keep the paragraph concise — this is the Methods summary, not the
  full derivation. Three to five sentences maximum.

### 1B — Supplemental Material: DALY Mathematical Details Subsection

Locate the Supplemental Material section in `who_cvd_targets_paper1.RMD`.
Add a new subsection after the existing supplemental mathematical content:

## DALY Calculation Methods

This subsection must be organized as follows and written in formal
epidemiological notation consistent with the Mathematical Notation Summary
already in the manuscript:

#### Years of Life Lost (YLL)

- State the YLL formula in display math `$$...$$`
- Formula must match exactly what is implemented in `07_output_dalys.R`
- Define all symbols: number of deaths (by age, sex, country, scenario),
  remaining life expectancy at age of death from the reference life table,
  any age-weighting function, any discounting factor
- State explicitly which life table standard provides the remaining
  life expectancy values — name the source, year, and geographic scope
  (e.g., "GBD 2019 standard life table, providing remaining life expectancy
  at each age for a frontier population")
- If YLL is computed separately by age group, sex, country, and scenario,
  show the full index notation

#### Years Lived with Disability (YLD)

- State the YLD formula in display math `$$...$$`
- Formula must match exactly what is implemented in `07_output_dalys.R`
- Define all symbols: number of prevalent CVD cases (or incident cases,
  whichever is used — derive from R code), disability weight, average
  duration of disability
- State explicitly which disability weight source is used — name the
  source, year, and the specific weight value(s) applied to CVD
  (extract exact values from `07_output_dalys.R`)
- State the duration assumption used — whether duration is modeled
  explicitly or assumed (extract from R code — do not assume)
- If YLD is computed separately by age group, sex, country, and scenario,
  show the full index notation

#### Disability-Adjusted Life Years (DALY)

- State the DALY aggregation formula: DALY = YLL + YLD in display math
- Show how DALYs averted are computed as the difference between the
  counterfactual and intervention scenario:
  DALYs averted = DALY(counterfactual) − DALY(intervention)
- This must match the differencing logic in `07_output_dalys.R`

#### Reference Standards and Weights — Parameter Table

Create a Word-compatible table (same package used elsewhere in the manuscript)
with these columns:
  Parameter | Value or Source | Year | Notes

Populate with:
- Life table standard name and source
- Disability weight for CVD (exact numeric value from R code)
- Age weighting applied (K constant if used, or "none")
- Time discount rate (r value if used, or "none — undiscounted")
- Duration of disability assumption

Add a footnote with full APA-format citations for each source.

#### Cross-reference

At the end of this subsection add:
"All DALY, YLL, and YLD results reported in the main manuscript are
computed using the above specifications, as implemented in
`07_output_dalys.R`."

---

## TASK 2 — MODIFICATIONS TO aim1_executive_slides.RMD

### BEFORE WRITING ANY SLIDES

1. Read `aim1_report.RMD` completely and catalog every object related
   to DALYs, YLLs, and YLDs — list their exact R object names,
   what they contain (scalar, table, figure), and the scenario/
   stratification they represent
2. Read the existing `aim1_executive_slides.RMD` to understand:
   - The slide separator syntax in use (e.g., `---`, `##`, or `###`)
   - The existing theme, layout, and chunk option conventions
   - Where in the slide deck to insert the two new slides (after the
     existing methods slide and after the existing results slides,
     respectively — place burden of disease slides adjacent to
     thematically related content)
3. Only after this inventory should you write the new slides

### SLIDE 1 — Burden of Disease: Methods

Add one new slide titled:
"Burden of Disease: Methods"

Content must include:
- A brief visual summary of the three metrics: YLL, YLD, DALY
  (use a simple bullet structure or a three-cell layout consistent
  with the existing slide style — do not add R execution)
- The DALY = YLL + YLD equation rendered in the slide format
  (use `$DALY = YLL + YLD$` inline math or display math consistent
  with the slide output format)
- The life table standard used (name only — one line)
- The disability weight source and CVD-specific weight value
  (extract from `07_output_dalys.R`)
- The discounting and age-weighting assumptions
  (extract from `07_output_dalys.R` — state "undiscounted" if applicable)
- A footer note: "Full derivation in Supplemental Mathematical Details"
- No R chunks on this slide — all content is static text and inline math

### SLIDE 2 — Burden of Disease: Results

Add one new slide titled:
"Burden of Disease Averted: YLLs, YLDs, and DALYs"

Content must:
- Display the key DALY, YLL, and YLD results from `aim1_report.RMD`
  using inline R calls to the objects cataloged in the pre-writing
  inventory above (e.g., `r total_dalys_averted`, `r yll_combined_scenario`)
- Use only objects already defined in `aim1_report.RMD` — do not
  define new objects in the slide file
- If `aim1_report.RMD` does not yet contain a needed summary object
  (e.g., a total DALYs averted scalar or a scenario-comparison table),
  FLAG THIS EXPLICITLY in a comment at the top of the slide chunk:
  `<!-- NEEDS: object_name must be added to aim1_report.RMD -->`
  Do not create the object in the slide file itself
- Present results by intervention scenario (consistent with how other
  result slides present CVD deaths averted)
- If a figure object exists in `aim1_report.RMD` for DALYs averted,
  reference it on this slide using the same chunk call pattern used
  for other figures in the slide deck
- Keep the slide uncluttered — prioritize the two or three most
  impactful numbers (total DALYs averted under the combined scenario,
  YLL vs YLD split, scenario comparison)

---

## CONSTRAINTS
- Modify ONLY `who_cvd_targets_paper1.RMD` and `aim1_executive_slides.RMD`
- Do not add any computations, data loading, or object definitions to
  either file — all numbers flow from `aim1_report.RMD`
- Do not alter any existing sections, slides, figures, tables, or prose
  outside of the specific insertion points described above
- Do not alter YAML headers in either file
- Mathematical notation must be consistent with the existing Mathematical
  Notation Summary in `who_cvd_targets_paper1.RMD`
- Slide formatting must match the existing visual style and separator
  syntax of `aim1_executive_slides.RMD`
- All formulas must be derived from `07_output_dalys.R` — do not use
  textbook DALY formulas that differ from what is coded

---

## BEFORE YOU BEGIN — REQUIRED READING SEQUENCE

Complete all reading before making any edits:

1. Read `07_output_dalys.R` in full — extract:
   (a) the YLL formula as implemented
   (b) the YLD formula as implemented
   (c) the life table standard referenced
   (d) the disability weight value(s) and their source
   (e) any discounting or age-weighting parameters
   (f) how DALYs averted are computed (counterfactual minus intervention)

2. Read `aim1_report.RMD` in full — catalog:
   (a) all DALY/YLL/YLD objects by exact name
   (b) whether each is a scalar, vector, data frame, or plot
   (c) which scenarios and stratifications each covers

3. Read `who_cvd_targets_paper1.RMD` — identify:
   (a) the exact insertion point in the Methods outcomes paragraph
   (b) the end of the Supplemental Material section where the new
       subsection will be appended
   (c) the notation conventions in the existing Mathematical Notation
       Summary to ensure new symbols are consistent

4. Read `aim1_executive_slides.RMD` — identify:
   (a) slide separator syntax
   (b) chunk option conventions
   (c) position of existing methods slide and results slides
   (d) inline R call syntax used in other results slides

5. Produce an internal checklist:
   (a) YLL/YLD/DALY formulas confirmed from R code
   (b) parameter table values confirmed from R code
   (c) aim1_report.RMD objects available for slides (list by name)
   (d) any missing objects that need flagging (do not create them)

Only after completing this checklist should you begin editing either file.
Produce a summary of all changes at the end, organized by file and task.

##############################################################################33
##############################################################################

You are making targeted fixes to one file only to resolve flagged missing 
objects identified during a previous editing pass. Modify ONLY aim1_report.RMD.
Do not touch any other file.

---

## FILE TO MODIFY
- `aim1_report.RMD` only

## FILES TO READ BUT NOT MODIFY (context only)
- `aim1_executive_slides.RMD` — to confirm exact object names expected
  by the slides (the HTML flag comments at the top of the burden of disease
  slide are the authoritative list of what is needed)
- `07_output_dalys.R` — to confirm how DALYs, YLLs, and YLDs are computed
  and what intermediate objects are available to derive the missing scalars
- `who_cvd_targets_paper1.RMD` — to confirm object names used in the
  manuscript so nothing is broken by this change

---

## CONTEXT

A previous editing pass flagged the following missing objects via HTML
comments in `aim1_executive_slides.RMD`:

MISSING SCALARS (currently only saved to `output/paper_scalars.rds`,
not available to slides):
  - `total_dalys_all`    — total DALYs averted under combined scenario
  - `total_ylls_all`     — total YLLs averted under combined scenario
  - `total_ylds_all`     — total YLDs averted under combined scenario
  - `total_dalys_bp`     — total DALYs averted under blood pressure
                           intervention scenario only
  - `total_dalys_bpd`    — total DALYs averted under blood pressure
                           plus diabetes intervention scenario
  - `total_dalys_stat`   — total DALYs averted under statin
                           intervention scenario only

MISSING SAVED OBJECT:
  - `sl_bod_summary.rds` — a summary data frame of burden of disease
                           results by scenario, needed by the slides,
                           currently not saved to `output/slides/`

---

## TASK 1 — VERIFY SCALAR COMPUTATION

Before adding any export code, read `aim1_report.RMD` and confirm whether
`total_dalys_all`, `total_ylls_all`, `total_ylds_all`, `total_dalys_bp`,
`total_dalys_bpd`, and `total_dalys_stat` are already computed as named
objects anywhere in the file.

- If any scalar IS already computed under a different name, create an
  alias assignment (e.g., `total_dalys_all <- existing_object_name`)
  immediately before the export chunk — do not duplicate computation
- If any scalar is NOT yet computed, derive it from the DALY/YLL/YLD
  objects already present in `aim1_report.RMD` or from intermediate
  objects produced by `07_output_dalys.R` — read that file to confirm
  the correct aggregation logic (e.g., sum across countries, age groups,
  years for the relevant scenario)
- Do not invent values or use placeholder numbers
- Document each scalar with an inline comment stating what it represents
  and which scenario it corresponds to, e.g.:
  `# Total DALYs averted 2025-2050, combined BP + statin scenario`

---

## TASK 2 — CREATE sl_bod_summary OBJECT

Create a summary data frame object named `sl_bod_summary` that contains
burden of disease results by intervention scenario, suitable for display
in the executive slides.

Structure the data frame with these columns:
  scenario | total_dalys | total_ylls | total_ylds

Where:
- `scenario` is a character label matching the scenario naming convention
  already used elsewhere in `aim1_report.RMD` and `aim1_executive_slides.RMD`
  (read both files to confirm the exact labels in use — do not introduce
  new scenario label strings)
- `total_dalys`, `total_ylls`, `total_ylds` are the aggregated numeric
  values for each scenario, derived from existing computed objects
- Rows should cover at minimum: the counterfactual (BAU), the blood
  pressure scenario, the statin scenario, and the combined scenario
  Include additional scenarios only if they already exist in the model output

Derive all values from objects already computed in `aim1_report.RMD` or
available from `07_output_dalys.R` output — do not re-run model code
or reload raw data files in this step.

---

## TASK 3 — EXPORT TO SLIDES OUTPUT DIRECTORY

Add a clearly labeled export chunk to `aim1_report.RMD` that saves:

1. All six missing scalars packaged into a named list, saved as:
   `output/slides/sl_scalars.rds`

   The list must contain these named elements, in this order:
   list(
total_dalys_all  = total_dalys_all,
total_ylls_all   = total_ylls_all,
total_ylds_all   = total_ylds_all,
total_dalys_bp   = total_dalys_bp,
total_dalys_bpd  = total_dalys_bpd,
total_dalys_stat = total_dalys_stat
)

2. The summary data frame saved as:
   `output/slides/sl_bod_summary.rds`

### Export chunk requirements:
- Label the chunk clearly: `{r export-slides-bod-scalars}`
- Add a chunk comment at the top:
  `# Exports burden of disease scalars and summary for aim1_executive_slides.RMD`
- Check whether `output/slides/` directory already exists in the project
  before saving — if not, create it with `dir.create("output/slides/",
  recursive = TRUE, showWarnings = FALSE)`
- Confirm the export chunk is placed AFTER all scalar and summary object
  definitions — do not place it before the objects are created
- Confirm the export chunk is placed AFTER any existing `paper_scalars.rds`
  export chunk so execution order is logical
- Use `saveRDS()` for both files — do not use `save()` or `write_rds()`
  unless the rest of the file already uses those conventions consistently

---

## TASK 4 — VERIFY CONSISTENCY WITH MANUSCRIPT EXPORTS

After adding the new exports, check that:
- The existing `paper_scalars.rds` export in `aim1_report.RMD` is
  unchanged and still contains all objects the manuscript needs
- No object is renamed or removed that was previously exported
- The new `sl_scalars.rds` is additive — it supplements, not replaces,
  the existing paper scalars export
- If `total_dalys_all` and the other scalars are also used in
  `who_cvd_targets_paper1.RMD` via inline R calls, confirm they are
  still available through whichever mechanism the manuscript uses
  (shared environment, child document, or loaded RDS) — do not break
  any existing manuscript inline calls

---

## CONSTRAINTS
- Modify ONLY `aim1_report.RMD`
- Do not add model-running code, data loading from raw sources, or
  any computation that re-executes the simulation
- Do not alter existing chunks, object names, or export paths already
  in `aim1_report.RMD` — only add new code
- Do not modify the YAML header
- All new code must follow the coding style already present in the file
  (tidyverse vs base R, pipe operator choice, indentation convention —
  read the file and match it)

---

## BEFORE YOU BEGIN — REQUIRED READING SEQUENCE

1. Read `aim1_executive_slides.RMD` — locate and copy out every HTML
   flag comment to confirm the complete list of missing objects and
   their exact expected names

2. Read `aim1_report.RMD` fully and produce an inventory:
   (a) Are any of the six scalars already computed? Under what names?
   (b) What DALY/YLL/YLD objects exist that can be aggregated?
   (c) Where is the existing paper_scalars.rds export chunk?
   (d) Does output/slides/ already exist as a save path anywhere?
   (e) What coding style conventions are used (pipe, tidyverse, etc.)?

3. Read `07_output_dalys.R` — confirm the aggregation logic needed
   to derive each scalar from available intermediate objects

4. Produce an internal checklist before writing any code:
   (a) scalar name → source object → aggregation needed (or alias only)
   (b) sl_bod_summary columns → source objects → confirmed scenario labels
   (c) export chunk placement → after which existing chunk?
   (d) directory check needed? yes/no

Only after completing this checklist should you add any code.
Produce a summary of all changes at the end, listing every new object
created, every existing object aliased, and the exact file paths written.

#######################################################3
Please modify the attached HTML figure `cvd_model_flowchart.html` so it is faithful to the actual model logic in `06_run_scenarios_multiple.R`.

Goal:
Revise the figure to be more loyal to the implemented model, improve visual separation between blocks, and remove any elements that are not in the code. Do not hallucinate new modules, states, or pathways. Only show structures that are explicitly supported by the model code.

Only Files to modify:
- `cvd_model_flowchart.html`


Required changes

1) Make the four modeled CVD causes explicit
The figure must clearly show that the model tracks four CVD causes separately:
- Ischemic heart disease (IHD / ihd)
- Ischemic stroke (istroke)
- Hypertensive heart disease (hhd)
- Hemorrhagic stroke / intracerebral hemorrhage (hstroke)

These causes are explicitly coded in the model. Show them as parallel cause-specific model dimensions, not as a vague generic “CVD” block.

2) Remove unsupported disease-state detail
The current figure shows:
- Mild CVD
- Moderate CVD
- Severe CVD
- Post-event
- Mild → Moderate
- Moderate → Severe
- Any → Post-event

These are not part of the implemented model and must be removed.

The implemented state model is much simpler and should be represented as:
- Well
- Sick
- Dead

Use the variable names when helpful:
- well
- sick
- dead

If you want to show transition logic, keep it aligned with the code:
- Well → Sick via incidence / new cases
- Sick → Dead via cause-specific fatality
- Background mortality separately affects population survival
Do not invent extra sub-states or post-event pathways.

3) Remove or rewrite unsupported population/fertility content
The current figure includes age-specific fertility and a cohort-component population projection / CCPM section. That should not appear unless you can verify it is truly part of this run script and model logic.

The user’s model is an adult cohort model, so there should be no fertility module shown. Do not show births, fertility, or a general population-projection module unless directly supported by the code used here.

If that entire panel is unsupported, delete it and rebalance the layout.

4) Highlight all four intervention/risk-factor pathways actually included
The figure must clearly show the four intervention families that are included in the model:
- Antihypertensives / hypertension control
- Sodium intake reduction (salt reduction)
- Industrial trans fat elimination / TFA ban
- Statin uptake

Show them as intervention inputs that modify the cause-specific rates already in the model. Keep wording concrete and policy-relevant.

If helpful, note that antihypertensive control includes a diabetes-specific hypertension-control component in the code, but the main visual emphasis should remain on the four primary intervention groups above.

5) Improve visual separation and eliminate overlap
The current display has overlapping blocks and crowded spacing.
Please:
- increase whitespace between step boxes
- prevent labels and arrows from crossing text
- align columns and headers cleanly
- make block boundaries clearer
- keep the figure readable at normal browser zoom
- rebalance the canvas after removing unsupported modules

Do not just slightly nudge objects; redesign spacing so the diagram reads cleanly.

6) Keep the figure conservative and code-faithful
Important constraints:
- Do not add modules that are not explicit in the code
- Do not add fertility, births, migration, post-event states, severity ladders, or recovery states unless directly implemented
- Do not imply a richer natural-history model than what exists
- Prefer a simpler but correct diagram over a more elaborate but inaccurate one

7) Keep current steps but align better with conceptual structure
A simpler structure would be better, for example:
- Inputs / calibration data
- Initial adult cohort states (well, sick, dead)
-  Cause-specific transition/rate framework across the four CVD causes
- Intervention modules (antihypertensives, sodium, TFA, statins) modifying incidence and/or fatality
- Annual state updating / scenario outputs

This is only a suggestion. Final structure should follow the actual code.

8) Deliverables
- directly edit `cvd_model_flowchart.html`
- keep it as a self-contained HTML/SVG file
- preserve pan/zoom behavior if possible
- provide a short summary of what you changed and why
- explicitly list any figure elements you removed because they were unsupported by the model

Grounding notes from the code and current figure
Use these as hard constraints:
- The code tracks the four causes explicitly: ihd, istroke, hstroke, hhd
- The implemented state variables include well, sick, dead, pop, newcases
- The current HTML figure incorrectly includes mild/moderate/severe/post-event states
- The current HTML figure also includes a fertility / CCPM population-projection panel that should not be shown unless directly justified by the run script
- The figure should prominently show the intervention set: antihypertensives, sodium reduction, TFA elimination, statins

Please make the revised figure cleaner, more minimal, and more faithful to the actual runned model.

#################################################################################

Refactor `08_economic_value_calculation.R` to compute a Value of Statistical Life (VSL) analysis for this project.

Goal
- Modify **only** `08_economic_value_calculation.R`.
- Convert the workflow to a clean, idiomatic **data.table** implementation.
- Remove figures, plotting code, and any logic not directly needed for the VSL analysis.
- The script should compare **intervention scenarios vs baseline** using outputs produced by `06_run_scenarios_multiple`.
- The script should save an **`.rds` output with one row per country-year-scenario comparison** containing deaths averted, population, VSL calculation columns under multiple income elasticities, GNI, and WHO region.

Method reference
- Follow the VSL approach from the Nature Medicine paper cited in the file comments and here:
  `https://www.nature.com/articles/s41591-024-03248-4?fromPaywallRec=false#code-availability`
- Use the paper/code logic for income-adjusted VSL with alternative elasticities. The paper discusses sensitivity analyses using lower and higher income elasticities (for example 1.0 and 1.5), and the current script also already defines baseline/sensitivity elasticities. Preserve that general framework unless adaptation is needed for this project. :contentReference[oaicite:0]{index=0}

Project-specific inputs
- Scenario outputs come from: `06_run_scenarios_multiple`
- GNI file: `Data/raw/API_NY.GNP.PCAP.PP.KD_DS2_en_csv_v2_7203.csv`
- You may inspect the existing `08_economic_value_calculation.R` for useful constants or formulas, but the refactor should be tailored to this repository’s scenario outputs, not the original avoidable-mortality example.

Required behavior
1. Read baseline and intervention outputs from the existing scenario pipeline.
2. Compute **deaths averted** as intervention deaths relative to baseline, at minimum by:
   - country
   - year
   - scenario
   - and any other dimensions required before collapsing to country-year if needed
3. Produce a final country-year level dataset with at least these columns:
   - `location` or `country`
   - `iso3` if available
   - `year`
   - `scenario`
   - `deaths_baseline`
   - `deaths_intervention`
   - `deaths_averted`
   - `population`
   - `gni_pc_ppp`
   - `who_region`
   - VSL columns for each elasticity assumption, clearly named, e.g.:
     - `vsl_e1_0`
     - `vsl_e1_2`
     - `vsl_e1_5`
     - `economic_value_e1_0`
     - `economic_value_e1_2`
     - `economic_value_e1_5`
4. Save the result as an `.rds` file in the project output folder.
5. Keep the output ready for later table/figure production; do not generate tables or figures in this script.

Implementation requirements
- Use **data.table only** for data manipulation. Avoid dplyr/tidyr pipelines in the refactor.
- Keep the script self-contained and readable.
- Add brief comments explaining:
  - which scenario files are loaded
  - how baseline vs intervention are matched
  - how deaths averted are calculated
  - how GNI is merged
  - how WHO regions are assigned
  - how VSL is calculated under each elasticity
- Make the code robust to missing GNI values:
  - if a target year is missing, impute from the nearest available year for that country
  - document the rule in comments
- Do not change any other file.
- Do not add plotting libraries or figure export logic.
- Do not preserve legacy code unless it is directly useful for the VSL workflow.

Important adaptation guidance
- This is **not** the original avoidable mortality application from the paper. Adapt the VSL calculation to the model outputs from `06_run_scenarios_multiple`.
- Assume the core economic quantity for this script is the monetary value of **deaths averted** under each intervention scenario relative to baseline.
- If the scenario outputs are age-sex specific, do the comparison at the most granular level needed and then aggregate consistently to country-year-scenario.
- Use clear object names and avoid hard-coded file names when possible, except for the provided GNI file.

Deliverables
1. The fully refactored `08_economic_value_calculation.R`
2. A short summary in your final response covering:
   - what inputs the script expects
   - what output `.rds` file it writes
   - the final schema of the saved dataset
   - any assumptions you had to make about the structure of `06_run_scenarios_multiple` outputs

Before editing
- First inspect the current `08_economic_value_calculation.R` and the outputs/read paths used by `06_run_scenarios_multiple`.
- Infer the existing repository conventions rather than inventing a new structure.
- Keep the refactor minimal in scope: only this file, only VSL-related work.


##################################################################################

Refactor `08_economic_value_calculation.R` to compute a Value of Statistical Life (VSL) analysis for this project.

Goal
- Modify **only** `08_economic_value_calculation.R`.
- Convert the workflow to a clean, idiomatic **data.table** implementation.
- Remove figures, plotting code, and any logic not directly needed for the VSL analysis.
- The script should compare **intervention scenarios vs baseline** using outputs produced by `06_run_scenarios_multiple`.
- The script should save an **`.rds` output with one row per country-year-scenario comparison** containing deaths averted, population, VSL calculation columns under multiple income elasticities, GNI, and WHO region.

Method reference
- Follow the VSL approach from the Nature Medicine paper cited in the file comments and here:
  `https://www.nature.com/articles/s41591-024-03248-4?fromPaywallRec=false#code-availability`
- Use the paper/code logic for income-adjusted VSL with alternative elasticities. The paper discusses sensitivity analyses using lower and higher income elasticities (for example 1.0 and 1.5), and the current script also already defines baseline/sensitivity elasticities. Preserve that general framework unless adaptation is needed for this project. :contentReference[oaicite:0]{index=0}

Project-specific inputs
- Scenario outputs come from: `06_run_scenarios_multiple`
- GNI file: `Data/raw/API_NY.GNP.PCAP.PP.KD_DS2_en_csv_v2_7203.csv`
- You may inspect the existing `08_economic_value_calculation.R` for useful constants or formulas, but the refactor should be tailored to this repository’s scenario outputs, not the original avoidable-mortality example.

Required behavior
1. Read baseline and intervention outputs from the existing scenario pipeline.
2. Compute **deaths averted** as intervention deaths relative to baseline, at minimum by:
   - country
   - year
   - scenario
   - and any other dimensions required before collapsing to country-year if needed
3. Produce a final country-year level dataset with at least these columns:
   - `location` or `country`
   - `iso3` if available
   - `year`
   - `scenario`
   - `deaths_baseline`
   - `deaths_intervention`
   - `deaths_averted`
   - `population`
   - `gni_pc_ppp`
   - `who_region`
   - VSL columns for each elasticity assumption, clearly named, e.g.:
     - `vsl_e1_0`
     - `vsl_e1_2`
     - `vsl_e1_5`
     - `economic_value_e1_0`
     - `economic_value_e1_2`
     - `economic_value_e1_5`
4. Save the result as an `.rds` file in the project output folder.
5. Keep the output ready for later table/figure production; do not generate tables or figures in this script.

Implementation requirements
- Use **data.table only** for data manipulation. Avoid dplyr/tidyr pipelines in the refactor.
- Keep the script self-contained and readable.
- Add brief comments explaining:
  - which scenario files are loaded
  - how baseline vs intervention are matched
  - how deaths averted are calculated
  - how GNI is merged
  - how WHO regions are assigned
  - how VSL is calculated under each elasticity
- Make the code robust to missing GNI values:
  - if a target year is missing, impute from the nearest available year for that country
  - document the rule in comments
- Do not change any other file.
- Do not add plotting libraries or figure export logic.
- Do not preserve legacy code unless it is directly useful for the VSL workflow.

Important adaptation guidance
- This is **not** the original avoidable mortality application from the paper. Adapt the VSL calculation to the model outputs from `06_run_scenarios_multiple`.
- Assume the core economic quantity for this script is the monetary value of **deaths averted** under each intervention scenario relative to baseline.
- If the scenario outputs are age-sex specific, do the comparison at the most granular level needed and then aggregate consistently to country-year-scenario.
- Use clear object names and avoid hard-coded file names when possible, except for the provided GNI file.

Deliverables
1. The fully refactored `08_economic_value_calculation.R`
2. A short summary in your final response covering:
   - what inputs the script expects
   - what output `.rds` file it writes
   - the final schema of the saved dataset
   - any assumptions you had to make about the structure of `06_run_scenarios_multiple` outputs

Before editing
- First inspect the current `08_economic_value_calculation.R` and the outputs/read paths used by `06_run_scenarios_multiple`.
- Infer the existing repository conventions rather than inventing a new structure.
- Keep the refactor minimal in scope: only this file, only VSL-related work.

###############################################################################33
###############################################################################33
Task: Update the Aim 1 executive slides to incorporate the economic value results, with emphasis on the Value of Statistical Life Years (VSLY) expressed as a share of annual income.

You may modify only these three files:
1. scenarios/scenarios_aim1/aim1_executive_slides.RMD
2. scenarios/scenarios_aim1/aim1_report.RMD
3. 08_economic_value_calculation.R

Do not modify any other files.

Overall objective
Update the executive presentation for Aim 1 so it includes a concise but compelling economics section. The executive slides should highlight the economic value of avoidable mortality, especially VSLY as a share of annual income, and present this in a way appropriate for an executive audience.

Key implementation rule
The file scenarios/scenarios_aim1/aim1_executive_slides.RMD must not perform calculations. It should only consume tables/figures/objects that are prepared upstream.
All calculations and plot creation needed by the executive slides must be moved into or produced through:
- scenarios/scenarios_aim1/aim1_report.RMD
- 08_economic_value_calculation.R

Required tasks

1) Add a methods slide to aim1_executive_slides.RMD
Create one slide that briefly explains the economic valuation methods used in 08_economic_value_calculation.R. Keep it executive-level and concise, but accurate.
This methods slide should:
- describe the VSL/VSLY approach used in the code
- mention that VSLY results are presented as share of annual income
- cite Robinson et al. guidance as the methodological basis
- briefly mention alignment with prior published work, specifically:
  - https://www.nature.com/articles/s41591-024-03248-4
  - https://www.nature.com/articles/s41591-024-03253-7?fromPaywallRec=false

2) Add a regional economic value results slide
Add a slide in aim1_executive_slides.RMD showing a table with the regional economic value results.
Use the table/object corresponding to:
- 08_vsl_summary_table_e1_2_primary
from 08_economic_value_calculation.R

This should be prepared upstream and then consumed by the slides. Do not calculate the table inside aim1_executive_slides.RMD.

3) Add a results slide with the main dot plot
Add a slide showing a dot plot of:
- the main VSLY results
- VSL results
- sensitivity results

This plot should not be produced in 08_economic_value_calculation.R anymore.
Instead:
- remove the dot plot creation from 08_economic_value_calculation.R
- create the dot plot in scenarios/scenarios_aim1/aim1_report.RMD
- make it available so aim1_executive_slides.RMD can consume it

This slide should also include a strong headline highlighting the world result.

4) Modify the summary slide
Update the executive summary/conclusion slide so that the headline takeaway explicitly highlights:
- the world economic value of avoidable mortality
- especially as a share of annual income
- with emphasis on the main VSLY result

5) Refactor file responsibilities cleanly
Ensure the workflow is clean and modular:
- 08_economic_value_calculation.R should compute/export the economic results objects needed downstream, but should no longer produce the dot plot
- scenarios/scenarios_aim1/aim1_report.RMD should generate the dot plot and any slide-consumable derived display objects
- scenarios/scenarios_aim1/aim1_executive_slides.RMD should only load/use prepared outputs and render the presentation

What I want from you
Please:
1. inspect the existing structure of these three files
2. implement the requested changes
3. preserve the existing style and conventions of the project
4. keep naming consistent with the current codebase
5. avoid introducing unnecessary new files or duplicated logic

Important content priorities
- Executive audience: concise, visually clear, policy-relevant
- Highlight VSLY as share of annual income
- Include world headline result prominently
- Keep methodological language accurate but not overly technical
- Maintain consistency with the existing economic valuation workflow

Output format
Please provide:
1. a short summary of what you changed in each of the three files
2. the exact code edits
3. any assumptions you had to make
4. any objects/tables/plots that the executive slides now expect from upstream

Do not modify files beyond the three listed above.