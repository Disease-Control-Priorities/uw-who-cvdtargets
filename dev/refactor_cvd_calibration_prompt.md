# Claude Code task: refactor `uw-who-cvdtargets` calibration

Implement this in the current repository. This is an implementation task, not a review or plan.

## Objective

Refactor the active pipeline from `code/00_run_model.R` through `code/03_calibration.R` so stage 03 writes **final, calibrated annual transition probabilities** to `data/processed/adjusted_searo_part*.rds`. Each required row must identify a model location, calendar year, sex, modeled cause, and single year of age **20–95**, where `95` is the open-ended **95+** group. These files must be directly usable by the active intervention (`04*`), baseline (`05*`), and scenario (`06*`) stages.

Read the repository's active `04*`, `05*`, and `06*` files before editing, and trace which objects, columns, cause codes, year ranges, switches, and files they actually consume. Use the repository's real filenames. The relevant design context from a separate SEAHEARTS refactor is included below; **no SEAHEARTS files will be attached to this Claude Code task**.

## Relevant design context, fully specified here

The related project separated its early pipeline into an authoritative script 00, a stage 02 that prepared epidemiology and transition-probability inputs, and a stage 03 that calibrated them. Adapt the following *patterns* to this repository:

- Script 00 defined a single set of shared paths, locations, causes and cause codes, age/sex grids, historical and forecast years, calibration window, random-search bounds, parameter granularity, iteration count, random seed, objective weights, parallelism, and output names. Stage 03 checked for these settings rather than defining alternatives. Here, **infer the actual CVD-targets values from this repository**.
- Stage 02 made a named input contract (`tps_inpt_part*.rds`) with `location`, `year`, `age`, `sex`, full cause label, `Nx`, `ALL.mx`, `BG.mx.all`, `BG.mx`, `PREVt0`, `DIS.mx.t0`, `IR`, and `CF`; it retained the underlying epidemiological counts needed for calibration. It read raw inputs without writing derived data back into the raw directory, used explicit age-band mappings, and checked key completeness and duplicate rows. Its demographic denominators and cause labels were consistent with the calibration targets. Determine which existing mortality source, input objects, and forecast helpers apply in **this** project before changing them.
- Stage 03 loaded only the named stage-02 inputs and targets; calibrated by `location × sex × cause` (with age-band-specific or shared multipliers as configured); compared simulated deaths and prevalence with targets over a defined historical calibration window; and applied winning `IR`/`CF` multipliers directly to all appropriate annual rows. Its random search included the unadjusted factors of 1 as candidate zero so a worse fit was not selected. It exported per-stratum multipliers and baseline-versus-calibrated diagnostics for audit, while the `adjusted_searo_part*.rds` chunks contained the **actual adjusted probabilities**.
- Its probability guard kept `IR`/`CF` in `[0,1]` and enforced `IR + BG.mx <= 1` and `CF + BG.mx <= 1`, preferably preserving `BG.mx` and capping disease probabilities into the remaining headroom. Any unavoidable background change was flagged in diagnostics. It sorted by key, chunked complete location groups, and checked that the output preserved the intended input keys and schema.
- The related design prevented later baseline code from multiplying probabilities by the same factors again. In **this** request, also move the applicable projected secular-trend adjustments into stage 03 and remove the corresponding active duplicate operations from stage 05.

Do **not** import the other project's 11-country SEARO scope, seven causes (including diabetes and CKD), age `0:95` grid or age-zero cohort initialization, GBD-only mortality rule, WPP redesign, global-trend method, exact numerical tuning constants, file dependencies such as `R/engine.R`, or separate clinical intervention assumptions. This project models adults aged 20–95+ and its existing CVD targets; preserve its scientifically intended source hierarchy and scenario assumptions unless a specific inconsistency must be repaired.

## Edit scope

- Primary edits: `code/00_run_model.R`, `code/02_load_inputs.R`, and `code/03_calibration.R` (create `03_calibration.R` if absent).
- **Remove `code/03_clean_inputs.R`** after moving any still-needed active behavior into stage 03. Stop sourcing it from script 00. Do not leave the old `031_calibration.R`/`032_adjustments.R` chain on the active execution path.
- Make the smallest necessary changes to **active** `02*`/`03*` helpers if stage 02 or 03 cannot otherwise satisfy the contract.
- Change an `04*`, `05*`, or `06*` file **only where it actually reapplies a baseline calibration or secular-trend adjustment that stage 03 has already incorporated, or requires a minimal input-contract correction**. Do not refactor those stages or alter intervention effects. In particular, inspect `05_build_baseline.R`: its `run_adjustment_model` block multiplies `IR`/`CF` by `adjustments2023_age.csv`, and its trend blocks adjust `BG.mx`, `BG.mx.all`, and `CF`. Ensure each desired baseline adjustment is applied **exactly once**, in stage 03, and remove or disable the duplicate operation in stage 05. Merely changing a switch is insufficient if another active block still changes the same quantities. If an `04*`/`06*` file needs no change, leave it untouched.
- Do not modify raw inputs, unrelated code, or output/report scripts.

## Implementation requirements

1. **Single source of truth:** Define pipeline-wide paths, model locations, modeled causes/cause codes, sexes, age grid, historical/calibration/forecast/model years, forecast assumptions, calibration settings, run switches, and shared file patterns once in `00_run_model.R`. Use those settings in stages 02–03 and active helpers instead of competing local definitions. Preserve the existing CVD-targets scope and policy scenarios; determine the actual required locations, four modeled CVD causes, and horizon from the repository. Replace machine-specific active paths with repository-relative paths.
2. **Execution order:** Script 00 sources stage 02, then stage 03, then the existing downstream stages in their intended order. Retire `03_clean_inputs.R` from the active path. Retain any necessary stage-02 work from `02_load_inputs.R` and its helpers, but make the data handoff explicit and avoid reliance on stale workspace objects or stale processed files.
3. **Calibrate and finalize in stage 03:** Consume the stage-02 transition-probability inputs and the project's appropriate calibration targets. Apply the fitted `IR`/`CF` factors **to the output rows in stage 03**; do not export unadjusted probabilities and expect stage 05 to multiply them later. Produce an auditable factor/fit diagnostic if useful, but do not require any later stage to apply it. Preserve the existing well–sick–dead model and calibration objective unless a concrete defect prevents this contract; explain any necessary mathematical change.
4. **Secular trends:** Trace the existing `run_bgmx_trend`, `run_CF_trend`, `run_CF_trend_80`, and `run_CF_trend_ihme` paths and the forecast objects they use. Incorporate the selected baseline projection assumptions into the **stage-03 final annual probabilities**, in the right years and exactly once. Avoid applying a forecast trend to the historical calibration target years or accidentally compounding a percent change year over year. If a switch represents an alternative forecast rather than a second adjustment, preserve that distinction. Keep `ALL.mx`, `BG.mx.all`, `BG.mx`, `DIS.mx.t0`, `IR`, and `CF` internally coherent after projection, with an explicit description of their meanings; do not silently treat a cause-specific background rate as all-other-cause background mortality.
5. **Final output contract:** Write deterministic `adjusted_searo_part*.rds` chunks under `data/processed/` covering **every configured location × year × sex × modeled cause × age 20:95**. Preserve the downstream schema and cause-code conversion expected by 04–06. At minimum, make available `location`, `year`, `age`, `sex`, `cause`, `Nx`, `ALL.mx`, `BG.mx.all`, `BG.mx`, `PREVt0`, `DIS.mx.t0`, `IR`, and `CF`, plus any other columns those stages require. Use one age-95 row for 95+. Do not write duplicate keys or include unrelated causes as modeled transitions. Scope file discovery narrowly so stale `adjusted*` files cannot be silently mixed with current chunks.
6. **Validation:** Check uniqueness and completeness of the full output key; exact expected age and year coverage; required nonmissing, finite fields; nonnegative population and mortality; `0 <= IR, CF, BG.mx <= 1`; and `IR + BG.mx <= 1`, `CF + BG.mx <= 1`, allowing only documented numerical tolerance. Verify that stage 05 loads the stage-03 results without applying a second calibration or trend and that the 04–06 contracts still work. Preserve a meaningful comparison of uncalibrated versus calibrated fit, and fail clearly on missing inputs rather than filling essential data with zero.

Keep changes focused; retain `data.table` patterns and established naming where practical. Parse every changed R file and run through stage 03 and a downstream smoke check if the data/packages are available. If execution is blocked, report the exact missing input/package and run the strongest feasible static and deterministic checks. Finish with a concise file-by-file diff summary, output schema and coverage, test results, and any unresolved downstream incompatibility.
