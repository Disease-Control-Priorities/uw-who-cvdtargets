# Claude Code task: make the coherent mortality forecast the default and separate calibration from trends

Implement this change in the current `uw-who-cvdtargets` repository. Inspect the active `code/00_run_model.R` through `code/06_run_scenarios_multiple.R` pipeline, especially `code/023_get_tps_bgmx.R`, before editing. This is an implementation task, not a plan. Keep changes focused on the pipeline and its directly affected scripts.

## Intended pipeline

1. `code/03_calibration.R` performs **in-sample calibration only**. It estimates and applies the incidence (`IR`) and case-fatality (`CF`) calibration multipliers to the historical `tps_inpt_part*.rds` data and writes an explicit, calibrated, **untrended** `adjusted_tps` artifact (for example, `data/processed/adjusted_tps_part*.rds`). It must not load forecast files, project mortality, or apply secular trends. Keep the existing calibration algorithm/objective and calibration diagnostics. The `adjusted_tps` artifact must be produced by stage 03, never by the new trends script.
2. Create `code/031_calibration_trends.R`. It reads the saved `adjusted_tps` artifact, extends the calibrated 2019 baseline through 2050, applies the selected mortality projection to **future years only**, enforces the transition-probability guard, validates coverage/schema, and writes the final `data/processed/adjusted_searo_part*.rds` files. Stages 04–06 consume those final files as before. This script must be independently runnable after calibration, including when the calibration R session is gone; running it repeatedly must start from the untrended artifact and never compound trends.
3. Set the **default selected projection** to the coherent all-cause / modeled-CVD-cause / other-cause forecast produced in the “Coherent all-cause / modeled-cause / other-cause forecast” section of `code/023_get_tps_bgmx.R` (Li et al., 2019). The output is `data/processed/tps_mortality_coherent_forecasted.rds`, with `tps_mortality_coherent_validation.rds` for diagnostics. **This single coherent output supplies the default projected all-cause trend (`ALL.mx`), modeled-CVD-cause trends (`DIS.mx.t0`), and both background mortality quantities (`BG.mx` and `BG.mx.all`); the new trends stage must actually apply these to future baseline rows.** Do not default to the existing separate Lee–Carter trend files (`tps_bgmx_forecasted.rds`, `tps_bgmx_all_forecasted.rds`, `tps_bgmx_cvd_forecasted.rds`). **Preserve those forecasts, IHME, and their selection paths for sensitivity analysis.** The coherent implementation itself uses Lee–Carter component forecasts plus MinT reconciliation; selecting its reconciled output as the default does not mean removing that internal step. Do not silently substitute the standalone trend files for the coherent output.

## Forecast application and integrity

- The coherent file has `location == "Global"`, ages 20:95, sex, year, short cause codes (`ihd`, `istroke`, `hstroke`, `hhd`), and `ALL.mx`, `DIS.mx.t0`, `BG.mx`, `BG.mx.all` plus cumulative relative changes versus 2019. Map causes explicitly to the full names in the calibrated inputs; join on the complete sex/age/cause/year key. Apply coherent **2019-relative trends** to each country's own 2019 rates rather than replacing country levels with Global rates. Do not compound changes year to year. Handle a zero 2019 denominator deliberately and report the rule; never allow an `NA`, `Inf`, missing key, or duplicate forecast key to silently fall back to a flat trend.
- Preserve the conceptual definitions: `ALL.mx` is all-cause mortality; `DIS.mx.t0` is the modeled cause's population mortality rate (not the `CF` transition probability); `BG.mx = ALL.mx - DIS.mx.t0` on each cause row; `BG.mx.all = ALL.mx - sum(DIS.mx.t0)` across the four mutually exclusive modeled CVD causes for that country/sex/age/year. Inspect the historical inputs because the current `023` comments indicate that the legacy `BG.mx.all` may also exclude dementia. Make the 2019 anchor compatible with the four-cause hierarchy and validate the identities after projecting. Do not scale `BG.mx`, `BG.mx.all`, and `ALL.mx` independently if that breaks those identities. Explain in code how the projected cause mortality trend informs the **calibrated CF** trend; `DIS.mx.t0` and `CF` have different units. **Keep the current default `run_CF_trend_80 <- TRUE`: apply 80% of the coherent modeled-CVD-cause cumulative change to calibrated `CF` (`CF_2019 * (1 + 0.8 * percent_diff_cvd)`), not 80% of a legacy standalone forecast. Apply the coherent background and all-cause trends at their full projected change, as the current 80% setting pertains to CF.** Document how CF attenuation affects modeled outcomes versus the reference coherent mortality identity.
- Retain explicit sensitivity controls for turning background and CF trends on/off, using full versus 80% CF change, and selecting the legacy standalone or IHME forecast where applicable. Make method precedence unambiguous so contradictory old flags cannot silently override the coherent default. Keep all trend selection and application in `031_calibration_trends.R` (settings may be defined centrally in `00_run_model.R`). Ensure changing a sensitivity setting reruns **only** stage 031 and the downstream work needed, never the calibration search.
- Preserve the stage-03 calibration algorithm, row keys, four modeled causes, historical coverage, age 95 as 95+, downstream schema, chunk naming, and existing `04`–`06` behavior. Update stale comments and file globs in `04_define_interventions.R` and `05_build_baseline.R` as necessary; in particular `04` currently uses a broad `pattern = "adjusted"`, which could accidentally ingest both `adjusted_tps` and `adjusted_searo`.

## Avoid the expensive rerun

**Do not execute the full calibration, `00_run_model.R`, or the full 02–06 pipeline as part of this task.** Implement a way for a normal run to reuse valid existing `adjusted_tps` artifacts and run `031_calibration_trends.R` without sourcing `03_calibration.R` or rebuilding forecasts unnecessarily. If the repository currently has only the old, already-trended `adjusted_searo_part*.rds`, do not treat those as untrended inputs or try to reverse the trends. When available, reconstruct the new untrended calibrated artifact from the historical `tps_inpt_part*.rds` and existing `calibration_factors.csv` (applying factors once, with matching keys and checks) **without rerunning the grid search**. If those factors or required historical inputs are absent, fail clearly with the exact missing prerequisites and instructions for the one-time calibration, rather than silently recalibrating. Keep stale chunk files from contaminating either output set.

## Verification and report

- Run syntax checks and a small synthetic or existing saved-data check of stage 031 only; do not trigger the expensive calibration or full simulation. Check historical rows remain untrended, 2020–2050 coverage, complete joins, unique keys, nonnegative finite rates, coherent mortality identities, probability constraints, and idempotence across repeated trend runs. Check that stage 03 contains no forecast application and that the final `adjusted_searo` schema still matches downstream expectations.
- Report exactly which files changed, the default and sensitivity selectors, how calibration can be reused, which checks you ran, and anything you could not verify without the full data. If existing calibration factors/inputs are unavailable, implement the code path but state that it was not exercised.
