# Claude Code prompt: deterministic sensitivity analysis for WHO CVD targets

Implement a deterministic sensitivity analysis (DSA) in the existing `uw-who-cvdtargets` repository. Work in the repository itself, inspect its current files and paths, and execute a small pilot before any full multi-country run. The attached versions of `00_run_model.R`, `04_define_interventions.R`, `05_build_baseline.R`, `06_run_scenarios_multiple.R`, `aim1_report.Rmd`, and `aim1_executive_slides.Rmd` describe the intended pipeline; the repository's current counterparts are authoritative. Read the relevant portions of `07_output_dalys.R` and `08_economic_value_calculation.R` as well because the report consumes their outputs.

## Goal and strict file boundary

Create **only these three source files**:

1. `code/10_deterministic_dsa.R`
2. `scenarios/scenarios_aim1/aim1_report_dsa.Rmd`
3. `scenarios/scenarios_aim1/aim1_executive_slides_dsa.Rmd`

You may create generated artifacts under **`output_dsa/`** and **`scenarios_dsa/`** only. The two new source Rmd files live at the explicitly requested paths above; render their HTML/PDF and export report tables in `scenarios_dsa/`, and put model RDS files and intermediate/report/slide RDS and PNG artifacts in `output_dsa/`. Do not modify, overwrite, rename, or delete any existing `.R`, `.Rmd`, data, configuration, or other file. Do not write into `output/`, `scenarios/` (except for the two new Rmd files), `data/processed/`, or `data/raw/`. Existing pipeline inputs may be read. If a dependency cannot be computed without violating this boundary, report the precise blocker; never substitute ordinary `output/` artifacts or silently skip requested results.

Follow the modeling guidance in Briggs et al., *Model Parameter Estimation and Uncertainty: ISPOR-SMDM Good Research Practices Task Force-6*, *Value in Health* 2012, 15:835–842: https://www.sciencedirect.com/science/article/pii/S1098301512016592 . These are deliberately selected scenario values, **not** probability distributions or 95% uncertainty intervals. Label illustrative assumptions as such and report each scenario's assumptions alongside its results.

## Read the actual model before editing

Trace the dependency chain `00` → `04` → `05` → `06` → `07`/`08` → report → slides. Preserve the actual `06_run_scenarios_multiple.R` transition engine and its scenario definitions, country exclusions, cause/age/sex/year outputs, and mutually exclusive BP-with/without-diabetes mixture. The script `10_deterministic_dsa.R` should consume the outputs/inputs prepared by `04` and `05`, and build DSA execution around the current `06` implementation. **Mortality-trend scenarios require rebuilding the annual `b_rates` output of `05` for each trend regime, then rerunning `06` from those rates; a completed reference `b_rates` or its model outputs cannot be rescaled to recover the alternative trajectories.** Do not create a toy surrogate based on multiplying aggregate deaths averted or rescaling a previously saved 2050 result. Re-run the country-level projection for every DSA setting. Avoid running the unguarded full `06` script as a dependency if doing so writes into ordinary `output/`; adapt/copy the necessary current model functions within the new script while documenting their provenance and keeping all DSA paths isolated. Likewise, do not re-source `04` if it would rewrite `data/processed/`; read its validated target tables and other prepared inputs when available. For trend alternatives, reproduce the necessary `05` transformations safely inside `10`, based on the same common pre-trend rates, corrections, UNWPP population, and COVID adjustments, with no changes to source inputs. Preserve the model's order of intervention composition and the behavior of untreated baseline scenarios.

Before implementation, resolve and document these **actual code facts**:

- The BP targets produced by `04` are a long table keyed by `scenario_id`, `location`, `sex`, `subgroup`; `baseline_year = 2025`, `scaleup_start_year = 2026`, `target_year = 2030`. In `06`, `validate_htn_target_table()` currently rejects target years other than 2030, so changing a CSV field alone will not implement 2035/2040. The model uses `aim2_control_trajectory()` inside `calculate_antihypertensive_split()`; implement a DSA-safe target-year validation/trajectory there without changing ordinary `06`.
- `calculate_statins_impact()` has default adherence arguments of `1`, but the **active country-level call** passes `adherence_ir = 0.575` and `adherence_cf = 0.664`. There are also unused/conflicting-looking top-level statin target-year assignments; the active country call uses `statin_target_coverage = 0.50`, `statin_start_year = 2026`, and `statin_target_year = 2030`. Use the values actually passed in the country loop as the faithful current base case, and record them in metadata. The original claim that the active base case assumes 100% adherence is inaccurate. If useful, add an explicitly labeled **perfect-adherence reference** (1.00/1.00), distinct from the actual base case.
- `adherence_ir` and `adherence_cf` currently affect **statin incidence (primary prevention) and case fatality (secondary prevention)** within `calculate_statins_impact()`. The split BP-control function does not consume those adherence arguments. Never imply that changing these statin parameters reduces BP effects. If modeling medication adherence for BP as well, implement it as a **separate, explicitly labeled exploratory BP adherence assumption** with a coherent definition of effective incremental control and corresponding incidence/CF effects; do not silently apply a generic factor to final deaths or double count adherence already embodied in achieved BP control.
- The ordinary `aim1_report.Rmd` reads per-country model RDS from `output/out_model/`, a separate `dt_output_dalys.rds`, and, if present, separate `08_*` economic RDS files; it writes many report figures, Excel tables, `paper/*.rds`, and `slides/*.rds`. The slides read the report-produced artifacts, not raw model outputs. The DSA equivalents must follow this dependency direction and never read ordinary `output/` as a substitute.
- In the newly supplied `05_build_baseline.R`, the three flags are separate: `run_bgmx_trend` changes background `BG.mx` and the all-cause envelope `BG.mx.all` using `tps_bgmx_forecasted.rds` and `tps_bgmx_all_forecasted.rds`; `run_CF_trend` changes cardiovascular **case fatality `CF`**; with `run_CF_trend_ihme = FALSE`, the age/sex/cause annual `percent_diff` comes from `tps_bgmx_cvd_forecasted.rds`; `run_CF_trend_80 = TRUE` applies `CF *= (1 + 0.8 * percent_diff)` for years after 2019, whereas `FALSE` applies the full `CF *= (1 + percent_diff)`. With `run_CF_trend_ihme = TRUE`, `tps_bgmx_cvd_ihme.rds` supplies a different cause/year `percent_diff`; the 80% flag is **not used in that branch**. With `run_CF_trend = FALSE`, no trend is applied to `CF`. Distinguish these mechanisms in methods and metadata; 80% is not a multiplier on final CVD deaths or projected benefits.

## Scenario specification

Define a machine-readable DSA scenario manifest in `10` with stable `dsa_id`, label, DSA family, target fraction, target year, statin adherence IR/CF, background trend flag, CVD trend regime/input, and income-group mapping where applicable. Save the manifest to `output_dsa/` as RDS and to `scenarios_dsa/` as a reader-friendly table. Use a single source of truth for these inputs, report captions, and slide labels. All one-way comparisons should hold other parameters at the **actual active 00/05/06 base-case values**.

**Reference**: reproduce the active `06` call: full increment to the current BP target and statin target by 2030, statin adherence 0.575/0.664; other intervention settings unchanged. Its population, outcomes, and intervention labels must match an existing ordinary run within numerical tolerance when the same inputs are supplied. Keep one shared reference model result if it is identical across DSA comparisons.

**Partial target achievement, one-way**: 50% and 75% of the *increment above baseline*, achieved by 2030. For each BP subgroup/sex/location/scenario row, calculate `target_control_DSA = baseline_control + f * (target_control_original - baseline_control)` for `f ∈ {0.50, 0.75}`, without changing the baseline, subgroup proportions, or original allocation. In the statin pathway calculate `statin_target_DSA = baseline_statin_coverage + f * (0.50 - baseline_statin_coverage)` for eligible groups; respect existing caps and baseline >= target behavior. State that the diabetic BP 80% figure is the full-target endpoint, not the endpoint of a 50%/75% increment scenario. Preserve the fixed 2025 population basis used to allocate the 150 million BP-control target in `04`; report the resulting achieved additional people rather than claiming the partial runs still attain 150 million.

**Delayed implementation, one-way**: attain the *original full endpoint* in 2035 and 2040, respectively, then hold the achieved level through 2050. Keep baseline at 2025 and begin scale-up in 2026. Apply the delay to **both** the BP control trajectory and the statin incremental uptake trajectory; retain the same endpoint and other interventions' original settings. Validate annual 2025, 2026, 2030, 2035/2040, and 2050 trajectories. Inspect `calculate_coverage_by_year()` for its `start_year` versus `baseline_year` denominator and explicit post-target rule; ensure the DSA's timing is transparent, hits the declared target in its named year, and documents any necessary difference from the active reference convention. If a faithful base-case reproduction and exact target-year convention conflict, preserve the faithful reference and make the DSA convention difference visible, rather than silently changing the reference result.

**Statin adherence, one-way**: compare the actual 0.575/0.664 base case with (i) low: `adherence_ir = 0.50`, `adherence_cf = 0.65`, and (ii) moderate: `adherence_ir = 0.70`, `adherence_cf = 0.80`. Propagate the values through statin IR and CF equations, including their baseline-coverage denominators, and rerun the model. Describe these as assumed effective treatment persistence/adherence over the modeled horizon, not evidence that individual 50-year persistence equals a single number. Do not mislabel the actual 0.575/0.664 reference as 1.0. A perfect-adherence 1.0/1.0 comparison can be shown as an extra reference, never as the observed/base-case DSA anchor.

**Income-group-varying statin adherence**: use existing country income metadata and a documented, editable named mapping covering LIC, LMIC, UMIC, and HIC, with lower assumed adherence in LIC/LMIC and higher in HIC. Set illustrative starting values (IR/CF) of LIC 0.50/0.65, LMIC 0.60/0.70, UMIC 0.70/0.80, and HIC 0.80/0.90 **unless the repository contains a defensible sourced mapping**. Label these illustrative values, not empirical regional estimates. Match the project's income codes (e.g., `L`, `LM`, `UM`, `H`) explicitly; require one unique valid mapping for every modeled country, and fail/report unmapped countries rather than giving them a default HIC rate. Stratify outputs by income group and show the global aggregate. Income group is different from WHO region; do not call this geographic region-specific adherence unless actual WHO-region-specific rates are specified.

**Alternative cardiovascular mortality trends, one-way structural DSA (background trend always on)**: use the attached draft `RTSL100M-ScientificManuscript WG 20260204(1).docx` as a *related study's example* of a trend-sensitivity table, then verify the current model code and available processed inputs. Its Table S8 also includes a no-background-trend scenario, which is **outside the scope of this DSA**. Its prose is inconsistent about the number of alternatives and whether the Foresight series is GBD 2019 or GBD 2021; do not copy those claims or numerical results without checking the actual `tps_bgmx_cvd_ihme.rds` provenance. Keep `run_bgmx_trend = TRUE` in **every** DSA scenario, including implementation and adherence analyses. Compare these four **clearly named CVD trend regimes** with the current model's fixed intervention coverage/adherence and all other inputs held constant:

| `dsa_id` suggestion | Background trend (`run_bgmx_trend`) | CVD CF trend (`run_CF_trend`) | 80% factor (`run_CF_trend_80`) | IHME source (`run_CF_trend_ihme`) |
| --- | --- | --- | --- | --- |
| `reference` | TRUE | TRUE | TRUE | FALSE |
| `trend_full_cvd` | TRUE | TRUE | FALSE | FALSE |
| `trend_no_cvd` | TRUE | FALSE | irrelevant | FALSE |
| `trend_ihme_cvd` | TRUE | TRUE | irrelevant | TRUE |

The reference reproduces the **actual current `00` switches**: `run_bgmx_trend = TRUE`, `run_CF_trend = TRUE`, `run_CF_trend_80 = TRUE`, and `run_CF_trend_ihme = FALSE`. `trend_no_cvd` retains background `BG.mx` and `BG.mx.all` trends and turns off only the CVD `CF` trend. Do not implement `trend_no_bg_no_cvd`, `trend_cvd_only_80`, or any other scenario with `run_bgmx_trend = FALSE`. Do not call `trend_full_cvd` a new Lee–Carter fit if it merely uses the already supplied forecast series; describe it as the full unscaled CVD input trend. Do not assume the IHME input is interchangeable with that series or scale it by 80%. Continue historical 2017–2019 rates unchanged and preserve 2020–2021 COVID adjustments; after 2019, apply each annual trend to the appropriate rate columns using the same age, sex, cause, year merge keys and formulas as `05`. Recalculate rates from one common pre-trend state for each scenario, never successively applying trend factors to a previously modified rate table. For `trend_no_cvd`, check that post-2019 `CF` retains its pre-trend annual assumption (other fixed adjustments still apply) while `BG.mx` and `BG.mx.all` receive the **same background trends as the reference**. Check available IHME series through 2050, join cardinality, missingness, nonfinite/negative rates, and cause coverage. If the external series or its provenance is unavailable, mark the IHME scenario unavailable with a precise reason; do not invent or fill values.

For each trend regime, rerun **its own BAU and intervention projections**: `averted_dsa = dead_bau_dsa - dead_intervention_dsa` within matching country/year/age/sex/cause and DSA ID. Never compare an alternative-trend intervention against reference-trend BAU. Report cumulative 2026–2050 BAU deaths, intervention deaths, deaths averted, percentage of that regime's BAU, plus differences relative to the reference's deaths averted; provide the same stratification and downstream DALY/economic analyses as feasible. Show annual BAU and intervention mortality trends to expose any baseline differences. Call these structural/scenario sensitivity analyses, not intervals around a forecast or proof that one trend is optimal.

At minimum, implement the above one-way scenarios. Additional combined low-achievement/delay/adherence scenarios may be added **only if distinctly labeled** as multiway stress tests, with their own identifiers, never mixed into a one-way tornado chart. Keep sodium, TFA, background mortality trends, other case fatality parameters, and population forecasts at their reference settings throughout all one-way runs; change the CVD `CF` trend only in the explicitly named trend scenarios.

## Execution, storage, and downstream results

- Make `10_deterministic_dsa.R` executable independently once the ordinary `04`/`05` inputs already exist. Set repository-relative paths or an overridable repository root; do not depend on the developer's `C:/Users/wrgar/...` paths. Expose `pilot_country` and selected DSA IDs for fast smoke runs. A full run covers all eligible countries, six original intervention scenarios (`baseline`, `bp_no_diabetes_only`, `bp_diabetes_only`, `bp_combined`, `statins_only`, `all_interventions`) and 2025–2050, or precisely document the original broader year range when matching it. Preserve `htn_target_scenario = "aim1"` and all columns needed by the downstream scripts. Cache reusable *pre-trend* inputs by country safely, while rebuilding scenario-specific annual rates. Guard the cluster teardown and error handling; fail a run if expected countries/scenarios are missing, and record a completion manifest.
- Write per-country/per-DSA model RDS under `output_dsa/out_model/<dsa_id>/`, with `dsa_id`, original `scenario`, country and model columns. Keep results separated by DSA ID to prevent the report from accidentally binding different analyses without a DSA key. Log each country/scenario failure under `output_dsa/logs/`; never blend partial failures into global totals.
- Examine `07_output_dalys.R` and `08_economic_value_calculation.R` and implement their necessary DSA downstream calculations or a **safely parameterized execution confined to `output_dsa/`**, using the DSA runs as inputs. Save the DALY and economic RDS artifacts under `output_dsa/` with a DSA identifier and the schema the new report needs. Do not reuse base-case DALY/economic outputs in a DSA table. If those analyses require unavailable inputs, make the omission explicit in rendered documents and completion report, instead of printing a non-DSA scalar.
- In `aim1_report_dsa.Rmd`, preserve the substantive analyses, tables, figures, and scalars of `aim1_report.Rmd` for the reference and comparably for DSA results, including deaths, causes, age/sex, countries, WHO regions and income groups, ASMR, burden of disease, and economic results where their DSA inputs are available. Show scenario assumptions and changes from the *actual* reference in levels and percentages. Add focused DSA summaries, annual time series, 2030/2035/2040/2050 endpoints, a tornado/difference figure for the one-way scenarios, and a **Table S8-style mortality-trend comparison** with clearly stated source, background/CVD switches, scenario-specific BAU and intervention deaths, absolute and percentage deaths averted. Do not report deterministic spreads as confidence or credible intervals. Read only `output_dsa/` for modeled results and place all derived report/slide RDS and PNG files under `output_dsa/paper/` and `output_dsa/slides/`; write rendered report and exported tables to `scenarios_dsa/`.
- In `aim1_executive_slides_dsa.Rmd`, maintain the original slide deck's substantive coverage, charts, tables, scalars, and visual style, adapted for reference plus DSA results. It must **not calculate outcomes or recreate report tables/figures**: it may format/display values and include images, but loads every analytic scalar/table/plot from the DSA RDS/PNG manifest written by `aim1_report_dsa.Rmd`. Validate all required artifacts and identifiers before rendering and give an actionable error instructing the user to knit the DSA report first. Put rendered slide output in `scenarios_dsa/`.

## Verification and handoff

1. Confirm the working-tree diff contains only the three requested source additions and generated files confined to `output_dsa/` and `scenarios_dsa/`. Do not adjust `.gitignore`, original scripts, or original reports.
2. On a pilot country, compare the DSA reference against the ordinary `05` rates **and** `06` result for the same inputs: row keys, baseline `CF`/`BG.mx`/`BG.mx.all`, intervention labels, dead/newcases/pop, and selected annual effects. Explain any numerical mismatch before proceeding. Verify no intervention effect in the baseline; no pre-implementation increment; 2035 and 2040 endpoints; 50%/75% incremental targets; statin adherence affects statin-active scenarios but not BP-only scenarios; and no income-group mapping gaps. For trend DSA, independently check unchanged historical rates, 80% versus 100% CF `percent_diff`, IHME branch precedence over the 80% flag, `run_bgmx_trend = TRUE` and identical background mortality input trajectories in every regime, no CVD CF trend in `trend_no_cvd`, and within-regime BAU matching.
3. Check no duplicates on `(dsa_id, scenario, location, year, age, sex, cause)`; no unexpected missing or nonfinite model values; required source RDS schemas; country-count parity; and correct baseline joins for deaths/DALYs/economic differences. Validate sums of stratified outputs against global totals.
4. Render the DSA report **then** slides for a pilot. Verify the slides only load report artifacts, all images resolve, and both documents clearly label current active adherence. If computational resources/inputs permit, execute the full country run and render again; otherwise provide exact commands, what passed, what was not run, and any blocker. Do not claim full verification from a pilot.
5. Finish with a concise list of created files, scenario definition table, output paths, example run/knit commands, tests and results, and any residual modeling limitations. No changes to existing files.
