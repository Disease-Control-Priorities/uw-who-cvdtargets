# Claude Code prompt: Aim 1 probabilistic sensitivity analysis

Work in the WHO CVD targets repository. **Implement the code and run a ten-draw end-to-end smoke test.** Read the current repository files before editing, especially `code/04_define_interventions.R`, `code/05_build_baseline.R`, `code/06_run_scenarios_multiple_optimized.R`, `code/07_output_dalys.R`, `code/08_economic_value_calculation.R`, `scenarios/scenarios_aim1/aim1_report.Rmd`, `scenarios/scenarios_aim1/aim1_executive_slides.Rmd`, and `data/processed/ettehad_rr_bp_reduction_effects_bplttc_2021.xlsx`. The full methodological context needed for this task appears below. **No separate PSA notes file will be provided.** If local names differ, use the repository's current canonical filenames.

## Goal

Propagate **uncertainty in supported intervention-effect parameters** through the **actual Aim 1 model**. Produce point estimates and 95% *parameter-uncertainty intervals* (the 2.5th and 97.5th percentiles of matched Monte Carlo outcomes) for the estimates displayed in the Aim 1 report and executive slides. Keep the existing point-estimate model, scenario definitions, estimands, and calculation methods intact. This is probabilistic parameter uncertainty, distinct from stochastic patient variation, structural uncertainty, and uncertainty in unsampled inputs. Methodological context: [ISPOR–SMDM Model Parameter Estimation and Uncertainty, Task Force 6](https://doi.org/10.1016/j.jval.2012.04.014).

## Immutable files and isolated outputs

- **Do not edit, overwrite, reformat, or regenerate any existing `.R`, `.Rmd`, `.xlsx`, or input files.** Do not change `00_run_model.R`, 04, 05, 06, 07, 08, the original report, the original deck, or the BPLTTC workbook. Existing files are read-only reference implementations. Create `code/09_uncertainty_psa.R` and, only if necessary, additional **new** helper or test files. Derive or adapt downstream 07/08 calculations in new PSA files rather than sourcing an existing script if it would write to `output/`.
- Put **all new data/model/report outputs**, intermediate files, logs, figures, PDFs/HTML, and checkpoints under `output_psa/`. Never write to the existing `output/` or `scenarios/` trees, including through sourced scripts, `knitr` defaults, side effects, or a working-directory change. Preflight all paths before executing; fail fast if a write would escape `output_psa/`.
- Put the two **new source documents** under `scenarios_psa/scenarios_aim1/aim1_report_psa.Rmd` and `scenarios_psa/scenarios_aim1/aim1_executive_slides_psa.Rmd`. This resolves the request's conflicting examples under `scenarios/scenarios_aim1/`: the explicit isolation rule and the `_psa` slide filename take precedence. Do not modify the existing `scenarios/scenarios_aim1/` documents.
- Paths must be configurable relative to the repository root (no hard-coded personal Windows path). Record a before/after tracked-file diff and confirm that no existing source or input changed.

## Model integration: preserve the actual 06 equations

1. Map the complete flow from 04/05 inputs through 06's country/scenario runner, `project.all()`, BP and statin/TFA/sodium effect functions, transitions, 07 DALYs, 08 economic outcomes, report tables/figures/scalars, and report-produced slide `.rds` files. Identify the model's existing output schemas and all reported endpoints. Write a concise implementation note under `output_psa/` documenting each input, distribution, injection point, downstream endpoint, and any gaps in supported evidence.
2. Implement 09 as a standalone PSA entry point that **uses the same model algorithms as 06**, with iteration-specific parameter tables passed explicitly to its PSA-local functions. `project.all()` and the country/scenario wrapper must accept the draw table as an argument; propagate it to `calculate_antihypertensive_split(..., etihad_rr_table = draw_table)` and every other sampled intervention pathway. Despite its name, `ETIHAD_RR_BIN` is read from the **BPLTTC** workbook in this version. Do not allow sampled paths to fall through to deterministic globals. Preserve the 06 scenario names, target-table behavior, subgroup population mixture, sequential year transitions, and multiplication across independent interventions. Do not replace 06 with a surrogate formula or a post hoc interval around its point estimates.
3. Source 04/05 preparation only after verifying they have no writes into the old output tree; otherwise consume their existing read-only derived input objects or recreate their necessary preparation **inside the PSA path**. Do not simply `source()` 06 in normal execution: its execution guard starts the full deterministic country cluster and saves into `output/`. Explicitly validate source and destination paths.
4. Use `data.table` keyed joins, `:=`, `set*`, group aggregations, and reusable country-level context where correct. Carry integer `draw_id` through **every** PSA model result and downstream aggregation. Vectorize the parameter generation and independent strata/draw computations; batch several `draw_id`s where memory permits. The dynamic state update over successive years must retain its chronological dependence; a `draw_id` dimension alone does not justify a single flattened operation across years. Chunk by country and draws, avoid accidental Cartesian joins and shared-reference mutation (`copy()` when needed), and benchmark peak memory and runtime. A correct batched run with a small outer loop over years/countries is preferable to false vectorization. Parallelize without nested oversubscription; set sensible worker and `data.table` thread counts.

## PSA distributions and shared draws

### Context and source estimates already established for this project

The current model reads `Sheet1` of `ettehad_rr_bp_reduction_effects_bplttc_2021.xlsx` into a misleadingly named `ETIHAD_RR_BIN` object; `Sheet1` contains derived columns, while the source HRs and CIs are in **`Appendix S6`**. The user has explicitly selected **BPLTTC Appendix S6** for the primary BP PSA. Earlier suggestions to use pooled BPLTTC results or Ettehad cause-specific RRs as the primary BP draws are superseded. The model's manual multiplicative construction across traversed BP bands must be reproduced, while clearly documenting that Appendix S6 SBP strata are subgroup treatment-effect estimates, not independent successive treatment increments. This multiplication is an existing **structural modeling assumption**, not something established by the source article. Evaluate alternative effect-source or construction choices as separate structural sensitivity scenarios only, not random switches inside this PSA.

The expected 15 BPLTTC source parameters are the following **HRs per 5-mmHg SBP reduction**, with 95% CIs in parentheses. Use these values as a mapping/check against the actual workbook, **not as hard-coded replacements** for it:

| Appendix S6 outcome | 130-139 | 140-149 | 150-159 | 160-169 | >=170 |
| --- | --- | --- | --- | --- | --- |
| Ischaemic heart disease | 0.96 (0.88-1.04) | 0.94 (0.87-1.02) | 0.91 (0.83-0.99) | 0.87 (0.80-0.95) | 0.94 (0.88-1.01) |
| Stroke | 0.92 (0.83-1.02) | 0.90 (0.82-0.99) | 0.77 (0.65-0.92) | 0.86 (0.79-0.94) | 0.90 (0.84-0.97) |
| Heart failure | 0.85 (0.75-0.97) | 0.95 (0.85-1.07) | 0.88 (0.78-1.00) | 0.70 (0.61-0.80) | 0.89 (0.79-1.00) |

There are 42 marginal estimates in that sheet; the `<120` and `120-129` rows are not sampled for this intervention. Retain unused source rows in the parameter register with an explicit exclusion reason. A single Stroke source draw maps to both `istroke` and `hstroke`; the latter is a subtype extrapolation. A single Heart failure draw maps to `hhd` (and to `aod` only if the repository actually does so). `>=170` maps to both `170-179` and `180+` if both are present.

For a modeled BP category `b`, define the per-10-mmHg effect for outcome `c` and traversed band `j` as `RR10[c,j,m] = HR5[c,j,m]^2`. Replicate `Sheet1`'s cumulative formulas: `R_noDM[c,b,m] = product(RR10[c,j,m], j=140-149 through b)` and `R_DM[c,b,m] = RR10[c,130-139,m] * R_noDM[c,b,m]`; the corresponding subgroup effects are `1 - R_noDM` and `1 - R_DM`. For example, in the `150-159` IHD category: `R_noDM = HR5[140-149]^2 * HR5[150-159]^2`, and `R_DM` includes `HR5[130-139]^2` as an additional factor. Verify any category-specific exception or zero in the actual workbook before coding it.

Earlier candidate estimates for **other** parameter families (evaluate against 06 before using; none is an instruction to add an unmatched effect): CTT major vascular events RR 0.78 per 1 mmol/L LDL reduction (95% CI 0.76-0.80); for people with diabetes, IHD/MI or coronary death RR 0.78 (CI 0.69-0.87) and stroke RR 0.79 (CI 0.67-0.93); coronary death RR 0.80 (99% CI 0.74-0.87); stroke death RR 0.96 (95% CI 0.84-1.09). A generic coronary death RR cannot be used as a stroke conditional case-fatality effect, and a composite event RR cannot be applied to both event incidence and case fatality without an endpoint-consistent decomposition. Basios and colleagues reported good statin adherence (at least 80% medication use) of 57.5% (95% CI 48.6-66.3%) in primary prevention and 64.4% (59.6-69.2%) in secondary prevention; these are prevalence probabilities, not continuous efficacy multipliers. If a matched adherence parameter is actually modeled, draw on the logit scale and apply mixture `RR_population = (1-p) + p * RR_full`, subject to the current model's exact mechanism. Do not substitute any of these candidates for an unrelated 06 parameter just because a CI is available.

The present BP case-fatality constants flagged in prior review are `ihd=0.24`, `istroke=0.36`, `hstroke=0.76`, `hhd=0.20`, and `aod=0.047` (verify current code). Appendix S6 `Cardiovascular death` HRs are **mortality endpoints, not conditional case-fatality effects**. Leave those case-fatality constants fixed unless a separate endpoint-matched source with uncertainty is established. Likewise, do not draw policy targets, coverage trajectories, 2026-2050 horizon, scenario membership, ASMR formulas, atherosclerotic stroke fraction, GBD high-fasting-plasma-glucose attributable fraction used as a diabetes proxy, or baseline prevalence/incidence/case fatality/population/current coverage in this intervention-parameter-only PSA without an appropriate uncertainty specification. Pickersgill et al. (2022) is a deterministic modeling precedent and Watkins et al. (2025) reports sensitivity scenarios; neither supplies a joint PSA distribution by itself.

- **Primary BP efficacy:** use the actual Appendix S6 estimates represented in the workbook's `Appendix S6` sheet. Verify row labels/values and the `Sheet1` formulas first. The 15 unique source estimates used in the supplied design are the workbook's `Ischaemic heart disease`, `Stroke`, and `Heart failure` estimates for baseline SBP `130-139`, `140-149`, `150-159`, `160-169`, and `>=170` mmHg. If current workbook rows or actual 06 mappings differ, follow and document the current source mapping; never silently assume the provisional row count above.
- For each `draw_id` and each used source estimate, sample on the log-HR scale: `log(HR_5_draw) ~ Normal(log(HR_5_point), ((log(CI_upper_5) - log(CI_lower_5)) / 3.92)^2)` for a reported **95%** CI. Read the published estimate and confidence bounds from `Appendix S6`; do not invent bounds, misread 99% CIs as 95%, or treat already derived spreadsheet cells as independent inputs. Preserve any known covariance. Where it is unavailable, state the working independence assumption and optionally assess a prespecified correlation sensitivity separately.
- **Rebuild `Sheet1` for each draw**: square each sampled HR per 5 mmHg to obtain its RR per 10 mmHg; multiply the RR terms over traversed BP bins exactly as the workbook currently does; derive `cummulative_rr_nodiabetes`, `cummulative_rr_diabetes`, `effect_size_nodiabetes`, and `effect_size_diabetes` for every `(cause, bp_cat)` required by 06. No diabetes traverses to below 140; diabetes additionally uses the 130–139 term. Draw only the original 15 source parameters, never the squared or cumulative values. Share the one stroke estimate between ischemic and hemorrhagic stroke, heart failure with hypertensive heart disease (and `aod` only if the current mapping uses it), and the `>=170` estimate between the two top model bins. Reuse the **same draw per parameter for all countries, ages, sexes, calendar years, and scenarios within the iteration**. Keep the point workbook construction as the deterministic reference and assert numerical agreement with it.
- Inspect **all other intervention parameters actually active** in 06. Add distributions only when endpoint definitions and uncertainty sources match the modeled incidence or case-fatality transition. Maintain a machine-readable parameter register (source, estimate, interval and its confidence level, transformation, sharing/correlation, affected model equations). In particular, do **not** interpret Appendix S6 cardiovascular mortality HRs as conditional case-fatality RRs; do not automatically sample the existing BP case-fatality constants without separate endpoint-matched evidence. Do not change the statin incidence/case-fatality architecture, extrapolate an IHD effect to stroke mortality, or double count fatal events. Keep unsupported parameters fixed and visibly identify them. Do not draw policy targets, baseline epidemiology/demography, model structure, choice of effect source, or workbook derived cells merely to make the intervals appear broader. If BP efficacy is the only defensible parameter family in this pass, implement and label the results as **BP intervention-effect parameter uncertainty**, not a full model uncertainty interval.
- Use a reproducible seed and stable iteration identifiers. The baseline and all counterfactuals must share the same `draw_id`, fixed source inputs, and any relevant shared draw. Compute each draw's difference, ratio, cumulative sum, group total, and economic transformation **within that draw**; take quantiles *after* the derived estimand is formed. Never subtract marginal interval bounds or add separate intervention interval bounds. The 06 deterministic result remains the **reported point estimate**; also retain Monte Carlo mean/median for diagnostics.

## New outputs and both documents

Save versioned, self-describing `.rds` objects under `output_psa/`, including the sampled parameter table, draw-level model outcomes (or reproducible per-country chunks), draw-level derived endpoints, point estimates, interval summaries, and manifest with seed/draw count/parameter scope. Supply the same substantive endpoints used by the original report and deck: deaths and deaths averted, annual/cumulative trends, intervention and cause breakdowns, geographic and age/sex breakdowns, age-standardized measures where present, DALYs/YLLs/YLDs, and VSL/VSLY and related ratios where actually reported. Ensure 07/08 analogues read the **PSA** model output and write only to `output_psa/`; if a reported endpoint cannot be propagated, show a clear diagnostic and document why rather than inserting a guessed interval. Preserve baseline and reference series as appropriate; label fixed reference inputs and deterministic quantities clearly.

Create `aim1_report_psa.Rmd` with the same substantive analysis and coverage as the current Aim 1 report, adding a 95% interval beside each eligible point estimate in narrative scalars, tables, and figures (e.g., bands/error bars that correspond to the same grouping and years). Avoid reporting point estimates from the old `output/` next to intervals from a different model specification. The PSA report writes all slide-ready tables, scalars, plots, and their manifest to `output_psa/slides/`. Create `aim1_executive_slides_psa.Rmd` with the same substantive analysis and slide coverage as the existing deck and interval displays; it **only reads these precomputed PSA report artifacts**. It must not rerun model calculations, PSA aggregation, 07/08 logic, or the report. Check the original deck's optional/fallback artifacts and missing-artifact comments so the new deck does not fail silently or show placeholders. Render generated report/deck files into `output_psa/` without leaving `*_files`, TeX intermediates, figures, or caches in the source directories.

## Validation and execution

1. First run a deterministic `draw_id = 0` with point parameter values through the PSA-local model. For a representative country and **every scenario**, compare key transition-level results and downstream totals against the original 06, 07, and 08 results at explicit tolerances. Account for any historical implementation defects discovered in 06; do not silently fix existing model behavior in PSA alone. Stop and report mismatches rather than producing plausible looking UI.
2. Run a **ten-random-draw smoke test** with a fixed seed, representative country(s) or a manageable scope, and all current scenarios. Confirm unique `(draw_id, location, scenario, sex, age, year, cause, ...)` keys where applicable; no missing/unbounded/nonfinite probabilities or effects; no unintended extra rows, cross-draw contamination, point-estimate drift, or output outside `output_psa/`. Validate workbook reconstruction, BP subgroup mixture, shared-draw mappings, zero-effect/no-change invariants, and the numerical quantile calculations on small fixtures.
3. Test that 07/08-derived data, report, and slide deck run end to end and produce actual `.rds` artifacts and rendered documents with the expected sections, tables, plots, and scalars. If full geographic coverage is required for a given display, run the ten-draw test for the required coverage or clearly mark a limited-country smoke test and test the full output schema without fabricating global totals.
4. Ten draws are **only a software smoke test**, never publishable 95% uncertainty limits. Implement a configurable production run target (initially 1,000 draws, expandable toward 10,000 after runtime/memory assessment). Include a convergence check of the 2.5th/97.5th percentiles for headline outcomes versus draw count and document Monte Carlo error. Mark ten-draw document renders conspicuously `TEST — 10 draws; intervals not for reporting`. Production document rendering must refuse to label ten-draw quantiles as final 95% intervals.

When finished, report exactly which **new** files were created, which parameter families were included and fixed, comparison results against 06/07/08, ten-draw test scope and timings, report/deck render status, path isolation verification, remaining limitations, and command(s) for running the full PSA. Do not claim a full production PSA or publication-ready intervals after a ten-draw test.
